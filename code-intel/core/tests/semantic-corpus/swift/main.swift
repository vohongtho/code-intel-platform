public class UserService { public func getUser(id: Int) -> String { "" } public func saveUser(user: String) {} }
public struct Point { let x: Int; let y: Int }
public protocol UserRepository {}
public enum UserRole { case admin, user }
