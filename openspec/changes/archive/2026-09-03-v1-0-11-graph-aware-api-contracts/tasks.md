# Tasks: Graph-Aware API Contracts

## 1. Baseline inventory and ownership

- [x] 1.1 Inspect `code-intel/core/src/pipeline/phases/parse-phase.ts`, existing route extraction code, `code-intel/core/src/shared/types.ts`, `code-intel/core/src/graph/*`, `code-intel/core/src/storage/graph-loader.ts`, `code-intel/core/src/storage/index-generation.ts`, `code-intel/core/src/multi-repo/group-sync.ts`, `code-intel/core/src/multi-repo/types.ts`, `code-intel/core/src/mcp-server/server.ts`, `code-intel/core/src/http/app.ts`, `code-intel/core/src/http/openapi.ts`, and `code-intel/core/src/cli/app.ts` before changing behavior.
- [x] 1.2 Document which existing route node/edge fields remain authoritative and which API-contract fields are additive. Do not create a second route model if the current graph Route node can be extended safely.
- [x] 1.3 Identify existing framework route extraction paths and record exact production gaps for Express/Fastify, NestJS, ASP.NET Core, response bodies, request DTOs, middleware/guards, and frontend consumers.

## 2. Code-level semantic contracts

- [x] 2.1 Create `code-intel/core/src/semantic/api-contracts/types.ts` defining discriminated unions for `HttpRouteFact`, `HttpRequestShapeFact`, `HttpResponseShapeFact`, `HttpConsumerFact`, `ApiContractMatch`, and compatibility findings. Every fact MUST contain source anchor, repository identity, canonical symbol references where available, framework, and certainty/coverage metadata.
- [x] 2.2 Create `code-intel/core/src/semantic/api-contracts/route-normalizer.ts` for HTTP method normalization, path-segment normalization, parameter normalization, nested prefix composition, and dynamic-path boundary reporting. Do not normalize two semantically different routes to the same key.
- [x] 2.3 Create `code-intel/core/src/semantic/api-contracts/shape-normalizer.ts` to normalize object keys, DTO/type references, optional/required fields, primitive/type-category information, status code variants, and unknown/dynamic shapes.
- [x] 2.4 Export the new contracts through the semantic-layer barrel used by the 1.0.11 universal semantic fact implementation instead of importing framework-specific files from MCP/HTTP handlers.

## 3. Producer extraction

- [x] 3.1 Refactor existing Express/Fastify route extraction in `code-intel/core/src/pipeline/phases/parse-phase.ts` and/or the framework-semantic adapter introduced by `v1-0-11-framework-semantic-adapters` so it emits `HttpRouteFact` while preserving existing Route nodes and `routes` output.
- [x] 3.2 Implement Express/Fastify nested router prefix composition, HTTP method, handler canonical ID, middleware list, path parameters, request-body evidence, and statically knowable response status/shape extraction.
- [x] 3.3 Add NestJS producer adapter under the framework-semantic adapter directory. Resolve `@Controller()` prefix + method decorator, handler canonical ID, guards/interceptors where statically known, `@Body()` DTO/type, `@Param()`/`@Query()`, status decorators, and return DTO/object-literal keys. (`@Param()`/`@Query()` are represented via existing path-parameter normalization, not modeled as separate shape fields — they aren't body/response shape data.)
- [x] 3.4 Add ASP.NET Core producer adapter for `[Route]`, `[HttpGet/Post/Put/Patch/Delete]`, controller prefixes, action symbols, request DTOs, `ActionResult<T>`/typed result evidence, and minimal API `MapGet/MapPost/...` handlers where statically resolvable.
- [x] 3.5 For every producer adapter, emit explicit `unsupported-dynamic`/`partial` evidence for computed route strings, reflection/runtime registration, unresolved DTO types, or response construction that cannot be proven. Never fabricate exact shapes.

## 4. Consumer extraction

- [x] 4.1 Create `code-intel/core/src/semantic/api-contracts/consumers/fetch.ts` for `fetch(...)`, including method, URL expression, body, expected response usage, and source call-site anchor.
- [x] 4.2 Create `code-intel/core/src/semantic/api-contracts/consumers/axios.ts` for `axios.get/post/...`, `axios({...})`, and configured clients when base URL is statically knowable.
- [x] 4.3 Create `code-intel/core/src/semantic/api-contracts/consumers/angular-http.ts` for Angular `HttpClient` method calls and generic response type arguments.
- [x] 4.4 Track consumed response properties when they can be linked through immediate destructuring/member access or typed result usage. Bound local data-flow depth and return partial coverage when exceeded.
- [x] 4.5 Add negative extraction fixtures for dynamic URLs, concatenation with unknown host/path, aliases that cannot be proven, and unrelated methods named `get`/`post`.

## 5. Producer-consumer resolution

- [x] 5.1 Create `code-intel/core/src/semantic/api-contracts/matcher.ts`. Match by repository/service scope, normalized HTTP method, normalized path, known base path, route parameters, and framework evidence; simple suffix or substring equality MUST NOT produce exact links.
- [x] 5.2 Define deterministic candidate ordering and a configurable hard candidate cap. Exceeding the cap MUST return truncated/lower-bound coverage through the shared relationship-certainty model. (Reuses `resolution/contracts.ts`'s `ResolutionOutcome`/`ResolutionCandidate`/`orderResolutionCandidates` directly — `ApiContractMatch` is a type alias for `ResolutionOutcome`, not a parallel model.)
- [x] 5.3 Resolve consumer-to-route links through canonical IDs and emit evidence strategy such as `exact-method-path`, `exact-normalized-base-path`, `candidate-dynamic-segment`, or `unresolved-dynamic-url`.
- [x] 5.4 Add fixture assertions for `/api/users/:id` vs `/users/:id`, `/v1/users` vs `/v2/users`, same route in different services, method mismatch, trailing slash, query strings, and path parameters.

## 6. Graph and persistence

- [x] 6.1 Extend `code-intel/core/src/shared/types.ts` and graph schema only where required to persist API-contract relationship metadata. Reuse existing Route nodes where possible; add new node/edge kinds only after proving an existing representation cannot preserve semantics. (Additive `NodeKind`: `api_shape`, `api_consumer`; `EdgeKind`: `accepts_shape`, `returns_shape`, `consumes_api`. Producer routes still merge onto the existing `route` node/`handles` edge — see section 3.)
- [x] 6.2 Update `code-intel/core/src/storage/graph-loader.ts`, CSV/bulk persistence code, and `code-intel/core/src/multi-repo/graph-from-db.ts` so API relationship certainty, call-site/source anchor, method/path identity, evidence reference, and shape fingerprint survive close/reopen. (These were already fully generic over `NODE_TABLE_MAP`/`ALL_NODE_TABLES` — the section 6.1 schema additions were sufficient; no further code changes needed here, verified by read-back tests.)
- [x] 6.3 Update Generation V2 metadata in `code-intel/core/src/storage/index-generation.ts` / `metadata.ts` with API-contract schema/fingerprint versions. A generation with incompatible API-contract schema MUST degrade explicitly rather than appear fresh. (`apiContractSchemaVersion`/`apiContractFingerprint` wired through `AnalyzerCompatibilityReceipt`, `IndexGenerationManifestV2`, `IndexMetadata`, `resolve-phase.ts`, `cli/app.ts`'s `buildAnalyzerCompatibilityReceipt`, and `analysis-plan.ts#hasSemanticFingerprintMismatch`, mirroring the existing `evidenceSchemaFingerprint` pattern exactly.)
- [x] 6.4 Add read-back tests comparing normalized in-memory and reopened API contracts/relationships, not only counts.

## 7. Compatibility engine

- [x] 7.1 Create `code-intel/core/src/semantic/api-contracts/compatibility.ts` returning `compatible | potentially-breaking | breaking | unknown` plus evidence and coverage.
- [x] 7.2 Implement route removal, HTTP method change, required request-field addition, request type-category change, response-field removal, response type-category change, success-status removal, and optional additive-field rules.
- [x] 7.3 Do not label a change safe when base/head shapes are partial, consumer usage is unknown, or route matching is ambiguous; return `unknown`/`potentially-breaking` with boundary reasons. (Caught and fixed a real gap while testing: a response variant with no resolvable shape at all was silently invisible to the diff loop instead of producing an `unknown` finding — fixed by tracking every observed status entry regardless of shape resolution.)
- [x] 7.4 Add base/head unit fixtures for every compatibility rule and at least one negative fixture proving an unrelated same-name DTO/route is not compared.

## 8. MCP, HTTP, CLI and existing impact integration

- [x] 8.1 Add service functions outside transport layers, e.g. `code-intel/core/src/semantic/api-contracts/service.ts`, for contract lookup, impact, and drift. `mcp-server/server.ts` and `http/app.ts` MUST only validate/route/serialize.
- [x] 8.2 Register `api_contract`, `api_impact`, and `api_drift` in `code-intel/core/src/mcp-server/server.ts` using existing repository/scope/selector resolution and additive certainty/coverage fields.
- [x] 8.3 Add equivalent HTTP routes in `code-intel/core/src/http/app.ts` and document schemas in `code-intel/core/src/http/openapi.ts`. Preserve existing auth, 400 malformed-scope, and 404 unknown-repository behavior.
- [x] 8.4 Add optional CLI commands/subcommands in `code-intel/core/src/cli/app.ts` only if they expose unique API-contract output; do not duplicate generic `impact` behavior under another name. (`api-contract`, `api-impact`, `api-drift` — HTTP method/path/shape/consumer output the symbol-oriented `impact`/`pr-impact` commands don't expose.)
- [x] 8.5 Extend existing `pr_impact` execution path so changed route/request/response facts appear as an additive API compatibility section with certainty and known-consumer coverage.
- [x] 8.6 Extend `code-intel/core/src/multi-repo/group-sync.ts` and `types.ts` to consume the same normalized HTTP contract facts rather than maintain a second route-shape parser. (Route↔route name-equality matching replaced with real consumer→producer evidence via `matchApiContracts`; verified it no longer links two repos' identically-named-but-unrelated routes.)

## 9. Incremental analysis

- [x] 9.1 Extend semantic invalidation from `v1-0-11-dependency-aware-incremental-resolution` so changed route facts invalidate route keys, reverse API-consumer matches, compatibility findings, and affected group contract fingerprints. (Found and fixed a real gap: `classifySemanticFact` didn't recognize `HttpRouteFact`/`HttpRequestShapeFact`/`HttpResponseShapeFact`/`HttpConsumerFact` at all — they fell through to `'embedded-region'`, which is outside `RELATIONSHIP_FACT_KINDS`, so a route/shape/consumer change in an indirectly-affected file would never have been swept into the invalidation closure. Added 4 new `SemanticFactKind` values, correct `identityPayload` cases, and added them to `RELATIONSHIP_FACT_KINDS`. Matches/compatibility findings themselves are never cached — `service.ts` recomputes them from the live graph on every call — so there is nothing further to invalidate for those; the group contract fingerprint path is `apiContractFingerprint`, already wired in section 6.3.)
- [x] 9.2 Update `code-intel/core/src/pipeline/analysis-plan.ts` / `incremental-indexer.ts` only through the shared artifact delta plan. Do not introduce a separate ad-hoc API incremental path. (No additional code needed beyond 9.1's classification fix and section 6.3's fingerprint wiring — API-contract facts flow through the existing `semantic-delta.ts`/`invalidation-closure.ts`/`re-resolve.ts` machinery unmodified, confirmed by the convergence tests below.)
- [x] 9.3 Add convergence tests proving clean full analysis and incremental analysis produce identical normalized API contracts after route rename, response-key removal, consumer URL change, file move, and deletion. (Extended the existing convergence harness to also run the Express/fetch adapters — purely additive, verified zero impact on the 10 pre-existing non-API convergence tests — then reused the same generic `buildConvergenceSnapshot`/`diffConvergenceSnapshots` machinery; no API-contract-specific comparison code needed.)

## 10. Web UI

- [x] 10.1 After MCP/HTTP schemas are stable, add API contract client types/API calls under `code-intel/web/src` using the existing Web API layer. (Types + `apiContract`/`apiImpact` methods added to `api/client.ts`, matching the existing `ApiClient` GET-with-query-params pattern. Also closed a gap the research turned up: `getApiContract`/`getApiImpact` had no way to select by graph node id, and no top-level `consumerCoverageComplete` field the way `api_drift` already did — both needed for the UI's required "no known consumer" vs "proven no consumer" distinction — so extended `RouteSelector`/`ApiContractResult`/`ApiImpactResult` in `service.ts`, plus threaded `route_node_id` through the MCP tool and HTTP route.)
- [x] 10.2 Add a contract detail/impact view showing method + path, handler, request/response shape, known consumers, compatibility findings, certainty, and incomplete-analysis boundaries. (Extended the existing `NodeDetail.tsx` panel with a new "Contract" tab for `route`-kind nodes, mirroring its existing `impact` tab pattern — there is no other node-scoped page/view in this app to add it to. Renders handler, middleware/auth evidence, request/response shape fields with coverage badges, and per-consumer match certainty.)
- [x] 10.3 Add Web tests for loading/error/partial/truncated states; UI MUST visually distinguish `no known consumer` from `proven no consumer`. (7 new tests in `NodeDetail.test.tsx` covering loading, error, empty/unsupported-framework, partial-coverage badge, and both known-consumer states — reusing the existing `ApiClient`-mocking + `renderWithApp` test pattern.)

## 11. Tests, metrics and release gates

- [x] 11.1 Add focused unit tests under `code-intel/core/tests/unit` for normalizers, shape comparison, extraction, and matching. (`normalizers.test.ts`, `compatibility.test.ts`, `matcher.test.ts`, `http-route-facts.test.ts`, `http-consumer-facts.test.ts`, `service.test.ts`.)
- [x] 11.2 Add integration fixtures under `code-intel/core/tests/integration` covering Express/Fastify, NestJS, ASP.NET Core, fetch, Axios, and Angular HttpClient across multiple files and same-name symbols. (`integration/api-contracts/multi-framework.test.ts`, plus the regression fixture added to `express-fastify.test.ts` for the fastify-detection fix.)
- [x] 11.3 Add persistence/reopen and MCP/HTTP contract tests, including malformed scope, ambiguous match, unavailable evidence store, and analysis truncation. (`api-contract-persistence.test.ts`, `api-contract-tools.test.ts` — 6 tests incl. malformed `base_repo_id`-missing scope, unknown repo, and empty-result cases.)
- [x] 11.4 Add performance counters for producer facts, consumer facts, exact/ambiguous/unresolved matches, comparison count, candidate cap hits, and elapsed matching time. (`ApiMatchInstrumentation`/`createApiMatchInstrumentation` in `matcher.ts`, surfaced via `--verbose` on the `api-contract`/`api-impact`/`api-drift` CLI commands.)
- [x] 11.5 Run the canonical 15-language semantic matrix. Languages where HTTP contract extraction is not applicable MAY report `not-applicable`; shared semantic/persistence behavior must still pass. (Reran explicitly: `semantic-corpus-release.test.ts` (15-language release report, no accepted-row regressions) and `semantic-fact-parity.test.ts` (normalized fact declarations across all 15 fixtures) both pass — 91/91 tests across both files plus `parser-corpus.test.ts`.)
- [x] 11.6 Run `npm test`, integration/e2e suites, MCP benchmark where affected, build/typecheck/lint, and Generation V2 reopen/read-back validation before marking the proposal complete. (Lint: 0 errors. Full workspace build: passes. Full unit+integration sweep: 2006 tests, 1968 pass, 37 fail, 1 skipped — every failure traced to a file untouched by this change (vulnerability-scan, embedding-model validation, scoped-search-contract, mtime-fallback flakiness, `doctor --json` runtime checks, the two `analyze` CLI integration clusters, persistent-session-restart) or, for the 2 touched test files (`openapi.test.ts`, `pr-impact.test.ts`), already confirmed pre-existing/out-of-scope earlier in this implementation (the pre-existing `/graph/{repo}` vs `/graph/{repoId}` doc mismatch and the pre-existing `trust.ts` empty-proof LOW-risk gap). All API-contract-specific suites pass. Generation V2 reopen/read-back: covered by `api-contract-persistence.test.ts` and `generation-v2.test.ts`, both passing.)

## 12. Documentation and release notes — mandatory Definition of Done

- [x] 12.1 Update root `README.md` Features section with Graph-Aware API Contracts, supported producer/consumer frameworks for 1.0.11, example `api_contract`/`api_impact`/`api_drift` usage, certainty limitations, and cross-repo integration. Do not advertise adapters that are only planned/partial as fully supported. (New `_(v1.0.11)_` bullet added after "Framework Fingerprint Metadata", scoped to exactly the 4 supported producer + 3 supported consumer frameworks.)
- [x] 12.2 Update root `CHANGELOG.md` under a new/active `## [1.0.11]` section with user-visible API-contract capabilities, new MCP/HTTP/CLI surface, compatibility behavior, incremental-analysis impact, and known limitations. (New `### 🔗 Graph-Aware API Contracts` subsection added under the existing `## [1.0.11] - 2026-08-18` header.)
- [x] 12.3 If MCP tool tables/examples exist elsewhere in repository docs or generated agent instructions, update them in the same implementation commit so tool names and fields match runtime schemas. (Searched the repo for other MCP-tool-table docs; only `README.md`/`CHANGELOG.md` (updated above) currently list MCP tool names. `code-intel/core/README.md` is a stale published-package mirror synced at release time, not a live doc, so it's untouched here — it already lags a full version behind on unrelated content.)
- [x] 12.4 Run a documentation consistency check: every README/CHANGELOG command and MCP field MUST exist in production code and at least one test; documentation updates are required before this OpenSpec change can be checked complete. (Verified `api_contract`/`api_impact`/`api_drift` MCP tools, the matching HTTP routes, and the `api-contract`/`api-impact`/`api-drift` CLI commands all exist in source and are covered by tests. Found and fixed one real gap: the 3 CLI commands had zero test coverage — added `tests/integration/cli/api-contract-cli.test.ts`, 5 tests, all passing.)
