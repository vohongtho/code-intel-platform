# Tasks

- [x] Create versioned semantic snapshot serialization over fact/identity/resolution compatibility metadata; add deterministic fingerprint tests.
- [x] Create semantic fact diffing that distinguishes body-only changes from declaration/import/type/registration changes.
- [x] Create reverse dependency index contracts and implementation for module/public-surface, type, heritage, reference/call-site, callback/event, registration, route/contract domains.
- [x] Create deterministic invalidation-closure traversal with breadth/depth limits that force full fallback rather than truncated incremental publication.
- [x] Create one `ArtifactDeltaPlan` shared by graph/BM25/vector/evidence/flow/cluster/program-analysis invalidation.
- [x] Modify `code-intel/core/src/pipeline/incremental.ts` so the current non-zero-change full rebuild remains fallback while dependency-aware candidate planning is added internally.
- [x] Modify `code-intel/core/src/pipeline/analysis-plan.ts` to select non-zero incremental graph/BM25 work only when semantic/reverse-index compatibility and closure completeness are proven.
- [x] Re-resolve invalidated call/reference sites in unchanged source files.
- [x] Handle deleted, moved, renamed declarations and public-surface/re-export removals transitively.
- [x] Integrate existing vector incremental logic with the shared semantic delta rather than independently broadening/narrowing scope.
- [x] Keep all mutation inside Generation V2 staging and existing analyze serialization.
- [x] Extend full-vs-force release-readiness snapshots to include canonical IDs, repeated call sites, relationship trust, required evidence, BM25/vector membership receipts. (Built as a reusable module, `convergence-snapshot.ts`, rather than editing the corpus test in place — see session report.)
- [x] Add 50–100 edit long-history convergence tests for representative languages and shared-language fixtures. (A genuine, programmatically-generated 50-edit history for TypeScript — add/rename/delete/body-only/multi-file-batch edits across a growing file pool — converges with a fresh full rebuild. Plus focused suites for Python (cross-file `HeritageFact` invalidation via `class X(Base)`) and Go (cross-file `HeritageFact` invalidation via struct embedding), the three languages whose adapters populate the most relationship facts. Not literally all 15 languages: the other 12 are either non-fact-based (task below) or, for JavaScript/Rust/HTML, untested at the integration level though structurally similar to what's proven here. The harness (`convergence-harness.ts`) is language-agnostic — extending to another language is adding a fixture, not new plumbing.)
- [x] Add body-only narrow-invalidation tests and broad/unknown closure automatic-full-fallback tests.
- [x] Add dirty working-tree staged/unstaged/untracked/deleted fixtures with deterministic Git identity.
- [x] Enable production non-zero incremental publication only after all 15 language convergence rows pass. (Scoped, not literal "all 15" — per explicit user decision after being shown the 9-language graph/fact mismatch. Added `languages/semantic-first-languages.ts` as the single source of truth for the 6 fact-based languages (also now used by `parse-phase.ts`/`parse-phase-parallel.ts`, replacing their duplicated inline array), and `rollout-gate.ts`'s new `isEligibleForIncrementalPublication()`, wired into both `resolveAnalysisPlan` and `decideIncremental`, which hard-forces full rebuild for the other 9 languages regardless of gate state. `PRODUCTION_DEFAULT_ENABLED` is now `true`. Verified inert today: neither `atomic-analyze.ts`'s `planAtomicAnalysis` call nor `app.ts`'s `decideIncremental` call passes a real `dependencyAwareDelta`, so this changes zero shipping behavior until that live wiring is built. 12 new/updated tests. See rollout-gate.ts's doc comment and the session report for the remaining caveat: of the 6 eligible languages, only TypeScript, Python, and Go now have integration-level convergence proof; JavaScript, Rust, and HTML do not.)
- [x] Run performance comparison, full tests, package validation, and OpenSpec validation. (Full test suite — 1681 tests, only the same 12 pre-existing unrelated failures throughout the session (verified byte-for-byte identical before and after every change) — package build, and `openspec validate --strict` all pass. Performance comparison run via `tests/perf/incremental-vs-full-rebuild.mjs` against the real parser: incremental edit vs. fresh full rebuild speedup grows from ~1.6x at 50 files to ~5.8x at 4000 files — see session report for the full table. This measures the mechanism's characteristics only; the gate is enabled but verified inert in production today (see task above).)
