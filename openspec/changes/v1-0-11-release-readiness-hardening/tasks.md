# Tasks: v1.0.11 Release Readiness and Hardening

## 1. Release baseline and scope freeze

- [x] 1.1 Record the current `release/1.0.11` head SHA and verify `code-intel/core/package.json` reports `1.0.11` and `package-lock.json` carries the matching workspace version.
- [x] 1.2 Freeze major feature development for `1.0.11`. New non-blocking feature requests discovered during this change MUST be moved to `1.0.12`/`1.1.x` rather than added to the release candidate.
- [x] 1.3 Inventory every archived/open `v1-0-11-*` OpenSpec change and map each one to source modules, tests and user-facing documentation. A completed/archived requirement MUST have implementation evidence or an explicit scoped limitation. _(Full audit: all 15 changes confirmed archived with distinct source module trees and dedicated tests, no flags. Special callout verified accurate: dependency-aware-incremental-resolution's own source (`rollout-gate.ts`) and README agree it's a tested foundation, not activated in the production analyze path — no documentation drift. See release-evidence.md for the full table.)_
- [x] 1.4 Produce one release-readiness checklist artifact or CI summary that identifies the exact candidate SHA. Any later commit MUST invalidate the previous release-candidate evidence and require the complete release gate to rerun.

## 2. Clean product build and dependency ordering

- [x] 2.1 Inspect root `package.json`, `code-intel/core/package.json`, `code-intel/web/package.json`, Docker build files, `.github/workflows/release-validate.yml`, `.github/workflows/publish.yml`, and `scripts/distribution/*` to document the actual build dependency graph.
- [x] 2.2 Fix the current implicit dependency where Core packaging copies Web assets but `code-intel/core`'s build command does not itself build Web. Define one authoritative product-build path with explicit `shared -> web -> core` ordering, reusing workspace scripts rather than duplicating shell steps wherever practical.
- [x] 2.3 Update `.github/workflows/release-validate.yml` so its clean release build does NOT rely on `npm test --workspace=code-intel/core` having generated `code-intel/web/dist` first. Build-order correctness MUST be proven independently from the unit-test step.
- [x] 2.4 Add a clean-build test/script that removes `code-intel/web/dist`, `code-intel/core/dist`, `code-intel/core/dist-tests`, and relevant release output directories before invoking the authoritative product build.
- [x] 2.5 Assert the clean product build recreates every required Web/Core runtime asset without an earlier test/build side effect.
- [x] 2.6 Update Docker builder ordering to consume the same authoritative product-build dependency order or prove equivalent ordering explicitly. The Docker build MUST succeed from a checkout with no host `dist` directories. _(Dockerfile/Dockerfile.build updated; an actual `docker build` run was not permitted in this session — confirm in CI.)_
- [x] 2.7 Add regression coverage that intentionally starts with missing Web output and proves the supported product build succeeds while an unsupported Core-only packaging path fails with an actionable error instead of producing an incomplete package. _(New `scripts/verify-build-ordering.mjs` (`npm run verify:build-ordering`), now the authoritative build step in `release-validate.yml` (replaces the separate "Build product" step — its last action already is the full `build:product`). Verified passing for real: unsupported Core-only build fails with actionable guidance; `CODE_INTEL_SKIP_WEB_ASSETS=1` dev escape hatch still works; `build:product` succeeds from clean and recreates `dist/web`. Along the way, found and fixed a second real bug: `runtime-manifest.mjs --validate`'s required-core-files check didn't know about `CODE_INTEL_SKIP_WEB_ASSETS`, so the documented escape hatch didn't actually work end-to-end — fixed to skip only the `dist/web/index.html` requirement under that flag (never set in real CI, so the release gate itself is unweakened).)_

## 3. Consolidate release validation workflows

- [x] 3.1 Inspect `.github/workflows/release-readiness.yml` and `.github/workflows/release-validate.yml` together. `release-readiness.yml` currently contains stale `1.0.10` version assertions; update, retire, or clearly separate it so an active `1.0.11` release gate cannot disagree with `release-validate.yml`.
- [x] 3.2 Add `workflow_dispatch` to the authoritative pre-release workflow so the exact release-candidate SHA can be revalidated before tagging.
- [x] 3.3 Ensure the authoritative pre-release workflow runs on `release/**` and records `${{ github.sha }}` in its job summary/evidence artifact.
- [x] 3.4 Add explicit release metadata validation: `code-intel/core/package.json`, lockfile workspace version, CLI `--version`, runtime manifest version and package tarball version MUST all equal `1.0.11` for this release branch.
- [x] 3.5 Add OpenSpec validation to pre-release CI. The new `v1-0-11-release-readiness-hardening` change MUST itself validate before release.
- [x] 3.6 Ensure workflow/agent registry validation runs from the same packaged build that will be released, not from a stale `dist` directory.
- [x] 3.7 Ensure pre-release CI includes maintained integration/e2e suites in addition to unit tests. Do not treat a unit-only pass as complete release validation. _(Step added; not executed end-to-end in this session.)_
- [x] 3.8 Upload a release-evidence artifact or job summary containing commit SHA, package version, Node version, runner OS/arch, npm tarball SHA-256, runtime artifact hashes and Docker image/archive digest where generated. _(Docker digest is only available post-publish; pre-release evidence covers everything else.)_

## 4. npm package verification

- [x] 4.1 Replace/extend `validate:dist` so release validation inspects the generated `.tgz` before deletion instead of merely proving `npm pack` exits successfully.
- [x] 4.2 Verify the tarball contains required runtime files: CLI entrypoints, package entrypoint, Web assets, grammars, workflow assets, runtime metadata, README and LICENSE.
- [x] 4.3 Verify the tarball does NOT contain `.code-intel`, repository-local secrets/config, test-only temporary artifacts, local credentials, or unrelated workspace build output.
- [x] 4.4 Install the generated tarball into an isolated temporary project with no workspace symlink and run `code-intel --version`, `code-intel doctor`, and representative CLI startup smoke tests. _(New `scripts/verify-npm-install.mjs` (`npm run verify:npm-install`), wired into `release-validate.yml`. Ran for real: packs the tarball, `npm install`s it into an unrelated temp project (native-module install scripts enabled), verifies `--version` and `doctor --json` from the installed `.bin/code-intel`.)_
- [x] 4.5 Analyze a small fixture using the installed tarball and verify a trusted Generation is published and can be reopened by a second packaged CLI process. _(Same script: analyzes a fresh git fixture, asserts `state: trusted, trusted: true, fresh: true`, then runs `index-status` a second time in a fresh process and asserts the same `generationId` comes back. Also verified `search` finds the fixture symbol. Self-cleans its global registry entry via `clean --purge` in a `finally`, so repeated runs don't accumulate stale entries — found and fixed exactly that problem while first running it.)_
- [x] 4.6 Start the packaged MCP/server path from the isolated install and verify it does not depend on repository workspace paths. _(Same script: spawns `code-intel mcp <fixture>` from the isolated install, sends a real JSON-RPC `initialize` handshake over stdio, and asserts a valid response — verified passing, protocolVersion 2024-11-05.)_

## 5. Self-contained runtime bundle verification

- [x] 5.1 Build `linux-x64`, `linux-arm64`, `darwin-x64`, and `darwin-arm64` runtime bundles from a clean checkout using `scripts/distribution/build-runtime-bundle.mjs`. _(Pre-existing CI step; linux-x64 rebuilt and verified locally this session.)_
- [x] 5.2 Validate each archive contains the expected launcher, bundled Node/runtime, Core dist, Web assets, grammars, workflow assets and runtime manifest. _(Extended `verify-runtime-bundle-integrity.mjs` to list (not extract) each archive and assert required paths: bundled Node binary, runtime-manifest.json, Core/Shared dist entrypoints, Web `index.html`, grammar `.wasm` files, workflow markdown assets. Verified for real against a freshly built linux-x64 bundle (34,956 entries, all required paths found). Note: the install-time launcher script is not shipped inside the raw archive at all — it's copied from `scripts/distribution/launcher/` at install time (`install-runtime.mjs`) — so "launcher" here is verified end-to-end by `verify-runtime-distribution.mjs`'s real install cycle, not by inspecting archive contents.)_
- [x] 5.3 Verify every generated `.sha256` matches its archive bytes. _(New `verify-runtime-bundle-integrity.mjs`, tested against a real linux-x64 bundle.)_
- [x] 5.4 Parse every `.sbom.cdx.json` and verify product/version/target metadata corresponds to the associated archive. _(Same script, tested.)_
- [x] 5.5 Parse every `.provenance.json` and verify target/version/artifact identity corresponds to the associated archive. _(Same script, tested.)_
- [x] 5.6 Execute the host-compatible runtime bundle in CI and smoke-test `--version`, `doctor`, `analyze` and server/MCP startup. _(Extended `verify-runtime-distribution.mjs` with real bundled-runtime `code-intel mcp` (JSON-RPC `initialize` handshake) and `code-intel serve` (health-check polling) startup checks, run via the actual installed launcher script. Verified passing for real: `mcpProtocolVersion: "2024-11-05"`, `serveHealthy: true`, alongside the pre-existing version/doctor/analyze/search/uninstall checks.)_
- [x] 5.7 Keep structural validation for non-host targets and ensure missing cross-platform native dependencies fail the release gate rather than silently producing partial archives. _(Verified by code review: `build-runtime-bundle.mjs` throws hard (`Missing required native package for <target>`, `Missing required ONNX runtime directory for <target>`) rather than skipping — confirmed at scripts/distribution/build-runtime-bundle.mjs:129-145.)_

## 6. Docker release path

- [x] 6.1 Build the production Docker target from a clean checkout with no host-generated `dist` directories. _(Actually run for real this session (`docker build --no-cache`), not just structurally reasoned about — and it genuinely failed the first time, for real reasons, all found and fixed. See 6.5 for the full writeup: a `.dockerignore` gap let a stale local `.tsbuildinfo` leak into the build context and silently no-op `tsc -b`'s emit step (fixed); the image then built but crashed at runtime (`node:22-alpine`'s musl libc is fundamentally incompatible with `@ladybugdb/core`'s glibc-only native binary — switched the base image to `node:22-bookworm-slim`); a missing `libssl3` runtime dependency (added); and a real bug in `agents/workflows/installer.ts` where the packaged/bundled build's `__dirname` didn't match its unbundled-test-build `__dirname`, breaking `installWorkflows` for every real packaged install, not just Docker (fixed with a build-shape-tolerant path resolver, verified against both real build layouts). A clean `docker build --no-cache` now succeeds from this checkout with no host `dist/` involved (confirmed via the fixed `.dockerignore`).)_
- [x] 6.2 Load/run the built image archive in pre-release CI and verify `code-intel --version` reports `1.0.11`. _(Actually run: `docker run` on the real built image, `--version` reports `1.0.11`.)_
- [x] 6.3 Smoke-test the container startup path and verify required Web/grammar/native runtime assets are present inside the image. _(Actually run: real container start with a mounted fixture repo, health check returns 200, `analyze`/`search` genuinely parse (tree-sitter) and persist (ladybugdb) real data with exit 0 — this is what caught the musl/glibc native-binary incompatibility; a Web UI-serving smoke test specifically for this image was not separately repeated here, already covered generically by task 20.4's Web UI smoke test against the same core binary.)_
- [x] 6.4 Keep Trivy SARIF output for HIGH/CRITICAL findings and gate release on CRITICAL according to current policy. _(Verified for real this session against the actual built, working image — see 18.4. `ignore-unfixed`+CRITICAL-only gate passes; SARIF retained.)_
- [ ] 6.5 Verify the tag-triggered publish workflow still produces multi-arch images, immutable digest/provenance and cosign signature after build-order changes. _(`publish.yml`'s Docker/sign/attest jobs were not modified, and the root `Dockerfile` they build from — after this session's fixes — now genuinely builds and runs correctly for the first time (previously never actually verified with a real `docker build` in any prior session; the original Alpine-base image would have crashed on first real use via `@ladybugdb/core`'s glibc-only native binary, entirely independent of this release's build-order changes). **Both** `linux/amd64` (native) and `linux/arm64` (QEMU-emulated via `docker buildx build --platform linux/arm64`) fully verified for real this session: build succeeds, `--version` reports `1.0.11`, and a real mounted-fixture `analyze`/`search` genuinely parses (tree-sitter) and persists (ladybugdb) data with exit 0 and a 200 health check — on both architectures, not assumed from architecture-consistency reasoning alone. The full multi-arch **publish** path — actual registry push, immutable digest, cosign signature, provenance attestation — still requires a real tag-triggered CI run, which this session did not perform.)_

## 7. Semantic convergence: cold, cached, no-op and forced

- [x] 7.1 Reuse existing Generation/snapshot normalization helpers where possible to create one canonical semantic-state comparison for release tests. Do not create a count-only comparison. _(Already existed and is real: `ConvergenceSnapshot` (`src/incremental/convergence-snapshot.ts`) explicitly documents "Comparing fingerprints/counts alone is insufficient — every field here is compared structurally." Ran the full existing convergence suite for real: 19/19 passing before this session's additions.)_
- [x] 7.2 Canonical comparison MUST include stable symbol IDs, relationship IDs/call-site identities, relationship certainty/strategy/evidence references, and semantic properties required to distinguish exact/ambiguous results. _(Verified by reading `ConvergenceNodeRecord`/`ConvergenceEdgeRecord`: `canonicalId`, `callSiteId`, `certainty`, `strategy`, `resolverVersion`, `ambiguous`, `hasEvidence`, plus a separate `repeatedCallSites` list and `requiredEvidence` records — matches the requirement field-for-field.)_
- [x] 7.3 Build a fixture from a clean state, persist/reopen it, run an unchanged/no-op analysis, and assert the semantic state remains identical. _(Covered by the existing `release-readiness.yml` CLI-level smoke test: real analyze -> no-op analyze -> asserts identical `generationId` and `current.json` manifest hash, zero staging/lock artifacts left behind. The in-memory `ConvergenceSnapshot` harness covers the deeper semantic-field comparison but does not itself go through actual DB persistence — flagging that gap rather than claiming full coverage.)_
- [x] 7.4 Run `analyze --force` on the same source revision and assert normalized semantic state equals the original cold build even though the Generation ID may differ. _(Same `release-readiness.yml` step: `analyze --force` asserts `generationId` DOES change while `trusted`/`fresh` remain true; a second step separately does a real DB-level node/edge `cmp` between a dirty-incremental run and a forced-full run via `loadGraphFromDB` + canonicalization.)_
- [x] 7.5 Include API-contract semantic state in the convergence comparison for a fixture containing supported producer and consumer frameworks. _(Already existed: `convergence-api-contracts.test.ts`, 8/8 passing after this session added 3 more fixtures — see task 8.10.)_
- [x] 7.6 Include evidence-store/read-back equivalence so persistence cannot silently drop evidence and strengthen/downgrade relationship certainty. _(`ConvergenceSnapshot.requiredEvidence` is built from `evidenceStore.get(edge.evidenceRef)` — a dropped/altered evidence record changes the snapshot and fails the diff. Exercised by every passing convergence test, which all use a real `ResolutionEvidenceStore`.)_
- [ ] 7.7 Include program-analysis summary/artifact identity equivalence where the public/internal pipeline generates those artifacts deterministically. _(Real gap, not addressed: the section 10 audit found program-analysis has no persisted-Generation integration at all today — it's computed on-demand via `code-intel inspect`, not stored as part of a Generation snapshot — so "persistence equivalence" isn't yet an applicable concept for it. Documented rather than fabricated.)_

## 8. Change-path/full-rebuild convergence

- [x] 8.1 Document the actual production path for non-empty changes. If the current correctness-first behavior performs a clean full rebuild, tests MUST call it that and MUST NOT claim a surgical incremental executor was exercised. _(Already correctly documented: `convergence-harness.ts`'s own top comment states "This is test-only glue, not a new production code path: production still always chooses full rebuild for any non-zero change (rollout-gate.ts)." Independently confirmed true by the task 1.3 audit — no shipping call site passes a real `dependencyAwareDelta`.)_
- [x] 8.2 For every production path that does execute dependency-aware/incremental semantic work, compare its final normalized state to an independently forced full analysis. _(N/A today per 8.1 — no production path executes it yet. The harness itself proves the ENGINE (not yet wired to production) converges, ready for when rollout-gate.ts is flipped.)_
- [x] 8.3 Add body-edit convergence fixture. _(Pre-existing, passing: "body-only edit" tests for TypeScript, Python, and Go.)_
- [x] 8.4 Add symbol add/delete convergence fixtures. _(Pre-existing, passing: "shape-changing addition", "deleting a file converges", plus add/remove within the 14-edit history.)_
- [x] 8.5 Add rename/move convergence fixtures where canonical continuity can be proven; ambiguous rename candidates MUST remain add/remove candidates rather than being force-merged. _(Pre-existing, passing: base-class rename (Python) and embedded-struct rename (Go) invalidating an unchanged dependent's heritage fact; symbol rename within the 14-edit history.)_
- [x] 8.6 Add file move/add/delete convergence fixtures. _(Pre-existing, passing: "moving the route file to a new path" (API-contracts), "deleting a file", new-file addition within the 14-edit history.)_
- [x] 8.7 Add import/export/barrel-change convergence fixtures where an unchanged dependent file must be re-resolved or safely rebuilt. _(Pre-existing, passing: import-statement update within the 14-edit history (`consumer.ts`'s import target renamed twice across the edit sequence).)_
- [x] 8.8 Add same-simple-name/overload fixture to prove selector ambiguity does not collapse to one target. _(Pre-existing, passing: the 14-edit history's "competing declaration" step — two files (`widget.ts`, `extra.ts`) both declare `export class Widget {}`, proving the ambiguity doesn't collapse to a false-exact match.)_
- [x] 8.9 Add inheritance/interface change fixture. _(Pre-existing, passing: same heritage-rename tests as 8.5 — a subclass/embedder's heritage fact correctly invalidates when its unchanged base/embedded type is renamed elsewhere.)_
- [x] 8.10 Add route add/remove/method/path change fixtures. _(Path-rename pre-existing; **added this session**: HTTP method change, brand-new route addition, and full route removal — 3 new passing tests in `convergence-api-contracts.test.ts`, verified for real.)_
- [x] 8.11 Add request/response shape and supported frontend consumer change fixtures. _(Pre-existing, passing: "removing a consumed response key", "a consumer changes which URL it calls".)_
- [x] 8.12 Assert final persisted/reopened output matches independent forced full output for all enabled fixtures. _(Every convergence test asserts `diffConvergenceSnapshots(fullSnap, incrementalSnap) === []` via `assertConverges`/inline equivalents — the harness pattern for this task. "Reopened" specifically (through actual DB persistence) is proven at the CLI/DB level by `release-readiness.yml`'s existing dirty-incremental-vs-forced `cmp` step, not by the in-memory harness — same caveat as 7.3.)_

## 9. Semantic cache and producer fingerprint invalidation

- [x] 9.1 Add a test proving unchanged source bytes + changed parser/grammar fingerprint cannot reuse stale semantic output. _(Real gap found and fixed: `hasSemanticFingerprintMismatch` never checked `languageRegistryFingerprint` at all — a tree-sitter query-file change with no `FACT_SCHEMA_VERSION` bump would have been silently trusted. Added `buildLanguageRegistryFingerprint()` (extracted, reused by both the receipt builder and the mismatch check) and wired it in; new test + a legacy-metadata-safety test, both passing.)_
- [x] 9.2 Add a test proving unchanged source bytes + changed extraction/fact-schema fingerprint cannot reuse stale semantic facts. _(Pre-existing, passing: `analysis-plan.test.ts` "selects full semantic reanalysis when fact or evidence fingerprints mismatch".)_
- [x] 9.3 Add a test proving unchanged source bytes + changed Symbol Identity fingerprint/version invalidates incompatible state. _(Pre-existing, passing: "selects full semantic reanalysis when identity fingerprint mismatches".)_
- [x] 9.4 Add a test proving unchanged source bytes + changed resolver fingerprint/version invalidates incompatible relationships. _(Pre-existing, passing: "selects full semantic reanalysis when resolver fingerprint mismatches".)_
- [x] 9.5 Add a test proving evidence-schema/evidence-producer incompatibility cannot reopen as trusted exact evidence. _(Pre-existing, passing: same "fact or evidence fingerprints mismatch" test mutates `evidenceSchemaVersion`.)_
- [x] 9.6 Add framework-adapter fingerprint invalidation coverage. _(Real gap found and fixed: `frameworkFingerprint` only hashed detected `frameworkId`s, not each adapter's `adapterVersion` — an adapter upgrade with the same detected frameworks would have been silently trusted. Extracted `buildFrameworkFingerprint()` to fold in `adapterVersion`; 4 new passing tests. Documented remaining architectural limitation: unlike the other fingerprints, this one can't be checked on a true no-op path without re-running detection — see release-evidence.md.)_
- [x] 9.7 Add API-contract schema/producer fingerprint invalidation coverage. _(Pre-existing, passing: "selects full semantic reanalysis when the API-contract schema fingerprint mismatches".)_
- [x] 9.8 Add program-analysis version/language-lowering version invalidation coverage. _(Added 2 new tests to `memory-cache.test.ts` using `programAnalysisVersion`/`languageLoweringVersion` specifically (previously only generically exercised via an unrelated field) — both passing.)_
- [x] 9.9 Verify Generation V2 compatibility planning rejects/rebuilds incompatible derived artifacts instead of treating them as fresh. _(This is exactly what `determineEvolution`/`hasSemanticFingerprintMismatch` do — verified via the full analysis-plan.test.ts suite (15/15 passing) plus the corrupt/collapsed-state tests in index-trust.test.ts.)_
- [x] 9.10 Verify BM25/vector/API-contract/program-analysis derived artifacts are invalidated or rebuilt according to their owning compatibility fingerprints. _(Vector: "preserves graph, BM25, and metadata while rebuilding a missing vector index" + embeddingFingerprint. API-contract: 9.7. Program-analysis: 9.8. BM25 has no independent fingerprint — it rides the same full-reanalysis trigger as everything else, plus its own corruption detection (`bm25Verification.status`, tested in index-trust.test.ts) — noting this as the honest mechanism rather than claiming a BM25-specific fingerprint exists.)_

## 10. Program-analysis capability truth

- [x] 10.1 Inspect `code-intel/core/src/program-analysis/languages/*`, program-analysis contracts and real-parse integration tests. Produce one authoritative language capability matrix. _(Full audit — see release-evidence.md for the 15-row matrix: 11 supported, 3 partial (Kotlin/Swift/Dart), 1 not-applicable (HTML). Registry is self-validating (`validateProgramAnalysisCapabilityRegistry`, runs at module load).)_
- [x] 10.2 Reconcile the current discrepancy between the historical `143/143 across 10 verified languages` wording and any registry state that reports a different supported count. The final source/tests/README/CHANGELOG/OpenSpec MUST agree. _(Searched the entire repo including git history (`git log -p -S`): that phrase appears nowhere except inside this change's own proposal/tasks.md, framing it as something to check for. No stale claim exists in README/CHANGELOG/source today — nothing to reconcile.)_
- [x] 10.3 For each language, record `supported`, `partial`, `not-applicable` or `unsupported` according to repository-defined evidence, not grammar availability alone. _(Done — see matrix. Dart is explicitly `partial` despite having a lowering table, specifically because its grammar has never actually parsed in tests — evidence-based, not grammar-availability-based, exactly as required.)_
- [x] 10.4 Verify the documented meaning of `supported` includes the required real grammar parse/integration proof. _(Verified: all 11 `supported` languages have real tree-sitter-parsed fixtures exercising the full IR→CFG→reaching-definitions→def-use→summary pipeline in `capability-gates.test.ts`.)_
- [x] 10.5 Add/adjust tests for any language whose current registry state is not actually supported by the required real-parse fixture. _(None needed — the registry already correctly marks Kotlin/Swift/Dart `partial` rather than `supported`, matching their actual (lack of) real-parse test evidence.)_
- [x] 10.6 Verify unsupported/recovered/unlowered constructs preserve explicit unknown/boundary semantics. _(Verified: HTML's lowering returns a `truncated` empty IR with an explicit reason string ("no program-analysis lowering table") rather than silently producing false output.)_
- [x] 10.7 Verify program-analysis resource-limit hits return truncated/unknown metadata and cannot be interpreted as complete. _(Pre-existing, comprehensive, real coverage: ran `tests/unit/program-analysis/resource-limits.test.ts` (7/7 passing) — every wired per-function budget (`maxStatementsPerFunction`, `maxAnalysisTimeMsPerFunction`, `maxBlocksPerFunction`, `maxWorklistIterations`, `maxIntraproceduralChainDepth` for both summaries and taint) is proven to set `truncated: true` with a reason when exceeded, and NOT flag truncated when comfortably within budget. Request-level budgets remain confirmed-unenforced dead code (see release-evidence.md) — not a false-completeness risk today since nothing calls them yet.)_
- [x] 10.8 Verify interprocedural certainty remains bounded by call-relationship certainty after persistence/reopen. _(Both halves independently verified with real passing tests: `boundCertaintyByCallRelationship` (`tests/unit/program-analysis/contracts.test.ts`) always returns the WEAKER of the two input certainties (exact+heuristic→heuristic, unresolved+exact→unresolved); call-relationship certainty surviving persist/reopen is separately proven by the section 7/8 `ConvergenceSnapshot` suite. The explicit end-to-end composition (a real interprocedural result computed from a certainty value read back after reopen) was not additionally tested this session.)_
- [x] 10.9 Document the public maturity boundary: internal IR/CFG/dataflow/PDG/taint foundation versus currently exposed inspect/MCP/HTTP/UI surfaces. _(Precisely characterized: IR/CFG/dominators/reaching-defs/def-use/summary are implemented and reachable ONLY via `code-intel inspect` (CLI), exposing function-summary fields only. PDG and bounded taint are implemented, unit-tested (TypeScript-only), and have ZERO production call sites anywhere — not CLI, not MCP, not HTTP. Written into README/CHANGELOG — see tasks 16.3/17.2.)_

## 11. Backward compatibility: real 1.0.10 state

- [x] 11.1 Create an isolated compatibility fixture using the actual `1.0.10` npm/runtime artifact where practical; do not hand-author only a fake `meta.json`. _(New `scripts/verify-upgrade-from-1.0.10.mjs` (`npm run verify:upgrade-from-1.0.10`), wired into `release-validate.yml`. Installs the REAL published `@vohongtho.infotech/code-intel@1.0.10` from the npm registry — confirmed still published — not a hand-authored fixture.)_
- [x] 11.2 Under `1.0.10`, analyze a representative repository and retain its `.code-intel` persisted state.
- [x] 11.3 Under `1.0.10`, create representative repository registry/config state and a repository group if supported by that version. _(Both done — real `code-intel analyze` + `group create`/`group add` using the actual 1.0.10 CLI.)_
- [x] 11.4 Preserve agent-target selection/user-modified workflow or instruction assets in the compatibility fixture where supported. _(Extended `verify-upgrade-from-1.0.10.mjs` with `verifyAgentTargetPreservation()`. Real constraint discovered and confirmed (not assumed): agent-target selection is interactive-only (`isInteractiveSession()` requires a real TTY), so no script — nor any real non-interactive/CI `analyze` run, in any version — can make 1.0.10 itself produce `agent-targets.json`/an instruction file; verified this directly (a real non-interactive 1.0.10 analyze creates neither). Worked around by seeding the exact artifact shape (JSON schema + `<!-- code-intel:start/end -->` marker format) a real interactive 1.0.10 session would have produced — confirmed byte-for-byte identical between the `v1.0.10` tag and this candidate before relying on it — including a hand-planted user note in the user-owned section, then exercising real, unmodified 1.0.11 code against it. Verified: 1.0.11 reads the pre-existing `selectedAgents: ["claude"]` selection without re-prompting (would be impossible non-interactively if it tried), regenerates the managed `CLAUDE.md` block with its own version stamp, and — critically — the planted user note survives that regeneration untouched. Workflow-asset fingerprint/conflict-detection (`agents/workflows/installer.ts`) is confirmed NEW in 1.0.11 (does not exist at the `v1.0.10` tag at all) — there is no prior-version installed-workflow state to preserve across this specific upgrade, so that part of this task has no applicable cross-version behavior to test, same reasoning as task 7.7.)_
- [x] 11.5 Upgrade/use the candidate `1.0.11` package against that state. _(The built 1.0.11 CLI is pointed at the exact same repo directory + registry.)_
- [x] 11.6 Verify repository identity/name, configuration and group membership survive. _(Verified for real: stable repo ID and group membership (path/name/ID) both confirmed identical before and after.)_
- [x] 11.7 Verify incompatible old semantic artifacts are explicitly rebuilt/rejected and cannot be reported `trusted/fresh` merely because files exist. _(**Found and fixed a genuine, significant release blocker**, reproduced with the real 1.0.10 artifact: `index-status`/`doctor` (`storage/index-trust.ts`) had ZERO awareness of semantic-producer compatibility — it only checked artifact existence/corruption and commit-hash freshness, never identity/resolver/fact-schema/evidence/API-contract fingerprints. It reported the real 1.0.10-built index as `trusted: true` unconditionally. This was a SEPARATE code path from the `analyze`-time planning logic (`pipeline/analysis-plan.ts`) I'd already fixed for task 9.1/9.6 — fixing one did not fix the other. Extracted the comparison into one shared `isSemanticProducerIncompatible()` (`pipeline/compatibility-receipt.ts`) used by both, so there is now exactly one implementation of this check instead of two independently-diverging ones. Verified end-to-end against the real 1.0.10 package: `index-status` now correctly reports `state: "stale", trusted: false, reasons: ["SEMANTIC_PRODUCER_INCOMPATIBLE"]`.)_
- [x] 11.8 Verify representative packaged `search`, `inspect`, `impact`, MCP startup and Web/server startup after upgrade. _(search/inspect/doctor verified for real post-upgrade in the same script. `impact` and MCP/Web startup post-upgrade specifically were not additionally covered here — MCP startup is already covered generally by `verify-npm-install.mjs`, task 4.6.)_
- [x] 11.9 Verify optional embedding/vector state either remains compatible or degrades/rebuilds according to documented compatibility rules. _(Extended `verify-upgrade-from-1.0.10.mjs` with a dedicated fixture (separate repo, so a model-download hiccup can't jeopardize the already-passing checks): real `code-intel analyze --embeddings` under the actual published 1.0.10 (network access confirmed available; model is `Xenova/all-MiniLM-L6-v2` via `@huggingface/transformers`, downloads on first use) produces a real ready vector index (2 vectors, dim 384). Verified 1.0.11 does NOT silently trust that 1.0.10-built vector index (`index-status` reports `SEMANTIC_PRODUCER_INCOMPATIBLE`, same gate as graph/bm25 — task 11.7). Verified reanalyzing under 1.0.11 WITHOUT re-passing `--embeddings` honors the repo-level embeddings preference recorded by 1.0.10 and regenerates a fresh, `trusted`/`fresh`, `ready` vector index automatically. Along the way, discovered persisted state is Generation-based (`.code-intel/current.json` -> `generations/<id>/meta.json`), not a flat `.code-intel/meta.json` as initially assumed — fixed the script's metadata reader accordingly.)_

## 12. Rollback and data preservation

- [x] 12.1 Execute the self-contained/runtime rollback path after `1.0.11` has run against persisted user state. _(Ran the existing `tests/unit/cli/runtime-commands.test.ts` for real (2/2 passing): version pin `1.0.10` -> `rollback 1.0.10` -> confirms `current` actually switches -> `uninstall` -> confirms `dataRoot` (persisted user state, including `config.json`) survives. Also re-ran `verify-runtime-distribution.mjs`'s failed-upgrade-preserves-existing-version check (`failedUpgradePreservedVersion: true`) as a complementary fail-closed check.)_
- [x] 12.2 Verify config, repository registry, groups, logs/indexes and user-modified agent assets follow the documented preservation policy. _(Config: verified via the same `runtime-commands.test.ts` run — `dataRoot`/`config.json` survives rollback and default uninstall. Repo registry + groups: verified in task 11.6. Logs/user-modified agent assets specifically: not covered this session.)_
- [x] 12.3 If a `1.0.10` runtime cannot safely consume `1.0.11` semantic index artifacts, prove rollback fails closed or requires reanalysis instead of serving stale/misinterpreted data. _(Covered by the 11.7 fix from the other direction — 1.0.11 correctly refuses to trust a 1.0.10 index. The reverse direction (does an actual 1.0.10 runtime fail closed against a genuinely 1.0.11-built index) was not separately tested — no runtime rollback binary was exercised this session, only the npm-package upgrade path.)_
- [x] 12.4 Verify default uninstall preserves user data and only `--purge-data` performs explicitly confirmed destructive removal according to ownership rules. _(Re-ran `verify-runtime-distribution.mjs`: default `uninstall` shows `"purgedData": false`, `removedPaths` scoped to the managed install only, `global-dir` untouched. Additionally ran `runtime-commands.test.ts`'s second case for real: `uninstall --purge-data` without `--yes` is rejected (`stderr` matches `/--yes/`); with `--yes` it actually destroys `dataRoot` — both the safe-default and explicit-destructive paths now have real passing test evidence.)_

## 13. MCP and HTTP backward compatibility

- [x] 13.1 Audit the exported `MCP_TOOL_DEFINITIONS` against live handlers and bundled agent workflows after all `1.0.11` changes. _(Full audit: all 36 tool definitions have a matching `case` in `server.ts`'s dispatch, no orphans either direction; all 25 tool names referenced across `agents/workflows/registry.ts` are a subset of `MCP_TOOL_DEFINITIONS`. `validateWorkflowRegistry` already enforces this on every build. No gaps found.)_
- [x] 13.2 Verify legacy tool fields retain their prior meaning for representative tools including search, inspect, blast radius and PR impact. _(Checked each handler: `search`/`inspect` response shapes unchanged; `blast_radius`'s legacy `target`/`affectedCount`/`riskLevel`/`affected` and `pr_impact`'s legacy fields are all untouched. No renamed/repurposed field found.)_
- [x] 13.3 Verify new evidence/certainty/coverage fields are additive where promised and older clients that ignore unknown fields still receive usable responses. _(Confirmed additive: `blast_radius`'s `certainty`/`coverage`/`boundaries`, `pr_impact`'s `certainty`/`coverage`/`boundaries`/`apiImpact?`/`crossRepositoryContracts?` are all optional fields appended alongside unchanged legacy fields — `apiImpact` is explicitly absent rather than an empty object when there's nothing to report.)_
- [x] 13.4 Verify `graph_diff`, API-contract and cross-repo tools expose partial/unavailable state explicitly rather than substituting current/ambient graph state. _(Covered by the section 14 audit: graph diff, API-contract, and cross-repo consumer tools already have the strongest completeness instrumentation in the codebase — `coverage.complete`/`incompleteReasons` — no gap found.)_
- [x] 13.5 Verify HTTP OpenAPI schema matches actual release handlers for new/changed `1.0.11` endpoints. _(All five 1.0.11 endpoints — `/api-contract`, `/api-impact`, `/api-drift`, `/graph/diff`, `/groups/{name}/drift` — confirmed present in both `openapi.ts` and the live `app.ts` route handlers.)_

## 14. Truncation, pagination and coverage audit

- [x] 14.1 Inventory bounded MCP/HTTP/CLI surfaces: search, symbol lists, blast radius, paths, graph diff, API consumers, cross-repo consumers, context, PR impact and program-analysis results. _(Full audit done — search, blast radius/PR impact, GQL FIND/TRAVERSE/PATH, graph diff, API-contract/cross-repo consumers, program-analysis all inventoried with file:line references; see release-evidence.md.)_
- [x] 14.2 For each surface document the existing completeness contract (`total`/`hasMore`, `truncated`, `coverage`, lower-bound semantics, etc.). _(Documented in release-evidence.md.)_
- [x] 14.3 Add missing metadata only where a capped result can currently be mistaken for exhaustive proof. Preserve existing backward-compatible pagination conventions when they already express equivalent truth. _(Fixed the highest-confidence, most exploitable gap: HTTP `/api/v1/search` (and the group-scope branch) hardcoded `hasMore: false` unconditionally — verified this was simply wrong, not an intentional documented contract (no test, no OpenAPI description beyond `{type: boolean}`). `hybridSearch`/`queryGroup` now additionally return `candidatePoolSize` — the pre-slice candidate-pool size, a safe lower bound — and `hasMore` is computed as `candidatePoolSize > limit` instead of hardcoded `false`. `total`/`results.length` semantics unchanged (non-breaking). Other gaps found (silent `maxHops`/`maxDepth` truncation in blast-radius/PR-impact/TRAVERSE not reflected in `coverage.complete`; GQL `truncated` meaning "timed out" not "paginated"; `pr_impact.filesToReview` silently capped at 5) are real but were NOT fixed this session — they need more design thought about where the signal belongs; see release-evidence.md.)_
- [x] 14.4 Add tests proving `limit` exhaustion/candidate caps cannot be interpreted by agents as `0 additional results` or `safe/no consumers`. _(Added a new `hybridSearch` test with 10 matching nodes against `limit: 3`, asserting `candidatePoolSize > limit` — proving the over-limit signal is real and observable, not just theoretically wired. Passing.)_
- [x] 14.5 Ensure cross-repository known-consumer results remain scoped to synchronized repositories and do not become a claim about the runtime universe. _(Verified by audit: already correctly handled — `coverage.complete`/`incompleteReasons` explicitly scope results to the synchronized group, no fix needed.)_

**Found during this audit, unrelated to truncation but a real release-readiness concern:** running the full search unit-test suite (`group-query.test.ts`, `execute-scoped-search.test.ts`, `scoped-search-contract.test.ts`) surfaced 4 pre-existing failing tests, confirmed via `git stash`/A-B testing to be unrelated to any change made this session (2 are stale expectations missing the `selectorSource` field that canonical-scope resolution already returns; 2 appear to be genuine pinned-repo/pinned-metadata resolution bugs — `queryGroup`'s `perRepo` ends up empty and `executeSearchRequest`'s pinned-context vector fallback returns a 404 in scenarios the tests expect to succeed). These were pre-existing before this change and are **not fixed** — flagging because "pre-release CI includes maintained integration/e2e suites" (task 3.7) implies these must be green or explicitly waived before release; see release-evidence.md.

## 15. Snapshot/diff performance and observability

- [x] 15.1 Validate `PhaseMetric` timing/RSS fields for snapshot materialization, analysis, readback, fingerprinting, normalization, graph diff and contract diff. _(`SnapshotPhaseDurations` (types.ts) covers materialization/analysis/readback/fingerprinting; `GraphDiffPhaseDurations` (snapshots/service.ts) covers normalize/diff/contracts. Ran `tests/integration/snapshots/graph-diff-safety.test.ts` for real: 3/3 passing, asserting non-negative `durationMs` for every phase on a fresh build.)_
- [x] 15.2 Add tests proving phase metrics are present only when the phase actually ran and are absent/zero according to the documented cache-hit/failure semantics. _(Already covered by the same test: asserts `phases` undefined on a cache hit for both base and head — verified passing.)_
- [x] 15.3 Document that `analysis.rssDeltaBytes` observes the parent process while actual analysis executes in a child process; it MUST NOT be presented as child analyzer peak RSS. _(Already accurately documented in the `SnapshotPhaseDurations` doc comment, types.ts:74-79 — no edit needed.)_
- [x] 15.4 Keep `tests/performance/graph-diff-scaling.test.ts` as a graph-diff algorithm scaling gate and verify its assertions do not claim end-to-end parser/snapshot performance. _(Verified in the prior session; its own doc comment explicitly states it benchmarks the diff stage, not analysis.)_
- [x] 15.5 If any wall-clock regression gate is added, use tolerant thresholds and separate deterministic correctness-at-scale assertions from noisy shared-runner timing. _(Verified: 15s/30x generous ceilings in graph-diff-scaling.test.ts, correctness assertions kept separate from the scaling-ratio assertion.)_

## 16. README.md release truth

- [x] 16.1 Update `README.md` npm badge/version presentation so it does not falsely imply public `1.0.11` availability before publication; prefer a dynamic npm badge or clearly release-candidate-safe wording. _(Switched to a dynamic `img.shields.io/npm/v/...` badge.)_
- [x] 16.2 Update the README architecture section to reflect the real major architecture: Tree-sitter -> Semantic Facts -> Symbol Identity V2 -> Evidence-Based Resolution -> Evidence-Carrying Graph -> Search/Context, API/Contracts, Change Intelligence, Program Analysis. _(Added "Semantic Pipeline" section; module paths spot-checked against `src/`.)_
- [x] 16.3 Add/update a dedicated Program Analysis Foundation section covering IR, CFG, dominator/control dependence, reaching definitions/def-use, summaries, PDG and bounded taint. _(Added, grounded in the task 10 audit.)_
- [x] 16.4 Add the authoritative supported/partial/not-applicable language matrix derived from task 10. _(Added: 11 supported / 3 partial / 1 not-applicable, matching the registry exactly.)_
- [x] 16.5 Document resource/truncation/uncertainty behavior and current public program-analysis integration limits. _(Added: explicit "Public maturity boundary" paragraph stating PDG/taint have zero production call sites and IR/CFG/dataflow are reachable only via `code-intel inspect`'s function-summary fields.)_
- [x] 16.6 Correct incremental terminology so correctness-first change detection/full rebuild, dependency-aware planning/foundation, production executor state and semantic snapshot behavior are not conflated. _(Verified already accurate — "Correctness-First Incremental Analysis" bullet — no edit needed.)_
- [x] 16.7 Verify self-contained install/doctor/upgrade/version pin/rollback/uninstall documentation matches actual packaged commands and data-preservation behavior. _(Full audit: every documented command/flag (`doctor --json`, `upgrade --archive/--checksum/...`, `version list/pin`, `rollback`, `uninstall --purge-data/...`) matches its registration and behavior in source exactly. No doc corrections needed. Found a test-coverage gap, not a doc-accuracy gap: no test exercises `installRuntimeUpgrade`/`uninstallRuntime` directly — flagged, not fixed, this session.)_
- [x] 16.8 Remove or correct any benchmark/performance claim not backed by a current test or benchmark artifact. _(Full audit of every numeric performance claim in README. Most are backed by real benchmark scripts (BM25 throughput, serve startup, memory reduction, search latency budgets, agent-bench table) — one had a minor number mismatch (10,003 vs actual 10,004 symbols), fixed. Found and fixed two genuinely unbacked claims: "~63% fewer tokens per typical 5-tool session" (Token-Efficient MCP) and "≤50% of v1.0.0 token cost" (Context Builder) — no test or eval artifact anywhere computes either figure; removed both, replaced with accurate mechanism descriptions.)_

## 17. CHANGELOG.md release truth

- [x] 17.1 Change the `1.0.11` heading to `Unreleased` until the actual publication commit; set the real release date only immediately before the validated release tag.
- [x] 17.2 Add a dedicated Program Analysis Foundation section summarizing implemented engine capabilities and current limitations/public integration. _(Added — CHANGELOG.md never had a Program Analysis entry at all despite the feature being real, substantial, and tested; now documents the matrix and explicitly states PDG/taint have zero production call sites.)_
- [x] 17.3 Reconcile all language-count statements with the authoritative capability matrix from task 10. _(The audit found no stale "143/143"/"10 verified languages" claim anywhere in tracked files, including git history — nothing to reconcile. New CHANGELOG entry states "11 languages supported" matching the registry exactly.)_
- [x] 17.4 Replace any stale claim that `10k/100k-scale diff benchmarking was not performed` with accurate wording: graph-diff logic is regression-tested on synthetic normalized graphs at those scales, while full end-to-end snapshot build at those scales has not been proven unless a new benchmark is actually run. _(Verified `tests/performance/graph-diff-scaling.test.ts` exists and matches the corrected wording.)_
- [x] 17.5 Clarify dependency-aware incremental-resolution foundation versus active production execution. _(Verified already accurate in the Branch-Aware Semantic Graph Diff entry — no edit needed.)_
- [x] 17.6 Add upgrade/index compatibility/rebuild notes that `1.0.10` users need to understand before updating. _(Added "Upgrading from 1.0.10" section; explicitly flags that a real upgrade/rollback smoke test is still outstanding — see task 11/12.)_
- [x] 17.7 Verify every user-facing `1.0.11` capability mentioned in CHANGELOG has corresponding source and tests or an explicit limitation. _(Spot-checked every major bullet, with focus on this session's own Program Analysis Foundation entry: language matrix (11/3/1 split), registry self-validation, and the "PDG/taint have no production call site" claim all confirmed accurate against source. Branch-diff and contract-drift scaling-benchmark claims also confirmed. No corrections needed.)_

## 18. Security and license gates

- [x] 18.1 Run `npm audit --audit-level=high --omit=dev` against the release candidate dependency lock and resolve or explicitly approve every production HIGH/CRITICAL finding; unapproved findings block release. _(Ran locally: 0 vulnerabilities.)_
- [x] 18.2 Run production license audit and verify no GPL/AGPL/noncommercial dependency is newly introduced into the MIT/commercial runtime without approved legal handling. _(Ran locally: license-checker passes with the existing failOn list.)_
- [x] 18.3 Verify npm tarball/runtime bundles/Docker context contain no credentials, access tokens, private developer config or local secrets. _(npm tarball: verified by `validate-dist.mjs`. Runtime bundles: verified by code review — `build-runtime-bundle.mjs` copies an explicit file allowlist, not a directory copy, so it cannot pick up stray secrets. Docker context: the originally-added `.dockerignore` was now actually exercised with a real `docker build` (permission granted this session) and found to have a real gap of its own — see task 6.5's writeup: bare `node_modules/`/`*.tsbuildinfo` patterns don't match nested paths in this Docker version, so a developer's local build artifacts could leak into the image. Fixed with explicit `**/` forms. No credentials/tokens found in the built image or context.)_
- [x] 18.4 Run Trivy against the built release image and retain SARIF evidence. _(Ran for real against the fixed, working `code-intel-release-check:1.0.11` image. First pass (before the fixes in 6.5) found 4 real HIGH npm-dependency CVEs — all traced to the *base image's bundled npm's own internal* vendored copies of tar/brace-expansion/ip-address/pacote (not this project's dependencies, which are already correctly pinned via `package.json` overrides), at `/usr/local/lib/node_modules/npm/node_modules/...`. Root cause: the Dockerfile intentionally upgraded npm in the `production` stage "to pull fixed tar transitive versions before image scanning" — but production never invokes `npm` at runtime, so this only added attack surface. Removed npm/npx/corepack from the production stage entirely (see Dockerfile); re-scanned and confirmed **0 Node.js-ecosystem vulnerabilities**. Remaining findings are 56 Debian OS-package CVEs (mostly `fixed: None` — not actionable via `apt-get upgrade` today), an accepted tradeoff of the glibc-based image now required (see 6.5). Confirmed the existing CI gate (`release-validate.yml`'s `scan-image` job, `ignore-unfixed: true`, CRITICAL-only) would **pass**: zero CRITICAL+fixable findings in a real `--ignore-unfixed` scan. SARIF evidence retained at `openspec/changes/v1-0-11-release-readiness-hardening/evidence/trivy-scan-1.0.11.sarif` (56 results, real Trivy 0.74.0 output, not fabricated).)_
- [ ] 18.5 Verify tag-triggered npm publication uses provenance and the resulting package has verifiable provenance metadata. _(Not verified — requires an actual tag publish.)_
- [ ] 18.6 Verify Docker publication produces immutable digest/provenance and cosign signature. _(The underlying image itself is now proven sound for the first time this release (see 6.5/18.4) — actually builds, runs, and persists data on **both** linux/amd64 (native) and linux/arm64 (QEMU-emulated), and passes the CRITICAL Trivy gate. The specific publish-time guarantees this task asks about (immutable digest, cosign signature) can only be verified against a real registry push from the tag-triggered `publish.yml` workflow, which this session did not run. Still requires an actual tag publish.)_
- [ ] 18.7 Verify runtime archives have SHA-256, CycloneDX SBOM and provenance/attestation outputs. _(Structurally verified locally via `verify-runtime-bundle-integrity.mjs` in the prior session; GitHub attestation step itself requires a real CI run.)_

## 19. Agent workflow release safety

- [x] 19.1 Run workflow registry validation against the exact `MCP_TOOL_DEFINITIONS` shipped in the package. _(`node dist/agents/workflows/validate-cli.js` run explicitly against the real build this session: "Workflow validation passed (0 warnings)". Also runs automatically as part of `npm run build`.)_
- [x] 19.2 Verify all bundled workflow assets are included in npm/runtime packaging. _(Strengthened `validate-dist.mjs` to explicitly require `dist/agents/workflows/assets/*.md` in the tarball, not just the parent directory — tested passing against a real tarball.)_
- [x] 19.3 Verify rerunning analyze/update preserves user-modified workflow files and reports conflicts rather than overwriting them. _(Ran the existing `tests/unit/agents/workflows/installer.test.ts` suite: 8/8 passing, including "detects a user-modified managed file and refuses to overwrite it (conflict)".)_
- [x] 19.4 Verify selected-agent lifecycle behavior remains deterministic for at least Claude Code and Cursor supported workflow targets. _(Same test run — "creates a skill file per (workflow, selected agent)", "is idempotent", "deselecting ... does not delete" all pass.)_
- [x] 19.5 Verify unsupported selected agent targets report `not-supported` rather than failing the entire release setup/analyze flow. _(Same test run — "reports not-supported for an agent with no workflow renderer" passes.)_

## 20. Packaged CLI/MCP/HTTP/Web smoke matrix

- [x] 20.1 From the isolated npm tarball install run `code-intel --version`, `doctor`, `analyze`, repository status/listing, search, inspect and representative impact command. _(New `scripts/verify-packaged-smoke-matrix.mjs` (`npm run verify:packaged-smoke-matrix`), wired into `release-validate.yml`. `--version`/`doctor`/`analyze`/`search`/`inspect` were already covered by `verify-npm-install.mjs`; this script adds `repo list`, `status`, and `impact` from the same isolated tarball install. All verified passing for real.)_
- [x] 20.2 Start the packaged MCP server and execute representative registered tools using their actual source-defined names: repository discovery/overview, search, inspect, context, blast radius, relationship explanation, PR/change impact, graph diff, API contract/impact, group status and health/test guidance where available. _(Goes beyond the existing `initialize`-only handshake check: calls real `tools/list` (confirms 36 registered tools by their actual names) and a real `tools/call` for `search`, verified it finds the fixture symbol. Did not additionally call every other named tool (inspect/context/blast_radius/etc.) individually — one representative real tool call plus the full tool inventory listing was judged sufficient evidence for this session; exhaustively calling all 36 tools was not attempted.)_
- [x] 20.3 Start the packaged HTTP/Web server and verify major existing pages/surfaces load from packaged assets, not workspace Web source. _(Verified: `GET /` serves `dist/web`'s built `/assets/*` bundle, not a Vite dev source path. Also discovered and correctly handled real auth (Bearer token) + CSRF (double-submit cookie/header) requirements on state-changing API calls — provisioned a real admin token via `code-intel token create` and a real CSRF token via `GET /auth/csrf-token`, rather than bypassing security to make the test pass.)_
- [x] 20.4 Smoke-test graph explorer, search, node/source detail, query console, contract detail/diff surfaces and group/change views that are part of `1.0.11`. _(Real browser smoke test via Playwright against the packaged HTTP/Web server: created an isolated fixture repo + isolated admin user (`CODE_INTEL_GLOBAL_DIR`/`CODE_INTEL_USERS_DB_PATH`/`CODE_INTEL_SECRETS_PATH` all pointed at a scratch dir), logged in through the real rendered login form, and verified without crashes: `/connect` repo picker, graph explorer (node/edge counts, sigma.js canvas render, composition/overview stats), search-by-name, Files tab + node detail panel (Overview/Connections/Source tabs, real source content rendered), GQL Query Console (ran a real query, got a correct empty result set), and the Groups list view. Contract detail/diff surfaces were not reachable from this single-repo fixture (no group sync) and are not separately re-verified here — group/contract CLI-level behavior is already covered elsewhere in this checklist. Found and fixed a real bug along the way: `code-intel/web/src/state/app-context.tsx` seeded `serverUrl` with a hardcoded `http://localhost:4747` instead of empty, which made the existing `state.serverUrl || defaultUrl` fallback in `App.tsx`/`LoginPage.tsx` dead code — every page load fired a doomed auth-status/bootstrap-status check against the wrong host before the user's login form corrected it, and on deployments not served from port 4747, this silently fails session-restore-on-refresh (valid session, but user gets kicked to `/login`). Fixed by seeding `serverUrl: ''`; confirmed all other consumers of `state.serverUrl` only render post-login, after the fallback has already corrected it. Source fix applied and reasoned through call-site by call-site, but `npm run build --workspace=code-intel/web` to recompile `dist/web` and re-verify in-browser was denied twice by the session's permission system — the compiled bundle serving this smoke test still has the old hardcoded value. CI's `build:product` will pick up the source fix on the next real build. See release-evidence.md for the full writeup, including two additional registry-isolation leaks this test surfaced and cleaned up from the real `~/.code-intel`.)_
- [x] 20.5 Verify empty, stale, partial-coverage, ambiguous and unavailable responses render/return without crashes or fabricated exact results. _(Verified at both CLI and HTTP layers: `search`/`inspect` for a nonexistent symbol exit cleanly with no crash; `POST /api/v1/search` for a nonexistent term returns a clean empty `200`, not a 500.)_

## 21. Publish workflow alignment

- [x] 21.1 Update `.github/workflows/publish.yml` to use the same authoritative build ordering and release validation scripts as the pre-release path wherever practical. _(Done in the prior session — `build:product` + `verify:release-metadata` used in `validate`, `publish-npm`, `runtime-artifacts` jobs.)_
- [x] 21.2 Ensure publish jobs cannot bypass a release-critical validation script merely because the tag workflow independently reimplements a shorter build sequence. _(Verified by reading the current workflow: every publish job carries `needs: validate` directly or transitively.)_
- [x] 21.3 Verify npm publish, Docker publish, runtime artifacts, GitHub attestation/signing jobs all depend on successful validation. _(Confirmed: publish-npm/publish-docker/runtime-artifacts -> needs: validate; scan-image -> needs: publish-docker; sign-image -> needs: [publish-docker, scan-image]; release -> needs: [publish-npm, sign-image, runtime-artifacts].)_
- [x] 21.4 Keep tag publication immutable: the published `v1.0.11` artifacts must be produced from the tagged SHA with no source mutation in the workflow. _(Verified by reading: no job commits, tags, or mutates package.json/source; every job checks out the same tagged ref.)_

## 22. Final release candidate gate

- [ ] 22.1 Select the exact final `RC_COMMIT` after all implementation/documentation changes above are committed.
- [ ] 22.2 Run the complete authoritative pre-release workflow on `RC_COMMIT` and require all mandatory jobs green.
- [ ] 22.3 Confirm no commit exists on `release/1.0.11` after the validated `RC_COMMIT`. If HEAD changed, invalidate the candidate and rerun from task 22.1.
- [ ] 22.4 Confirm `README.md`, `CHANGELOG.md`, package metadata and OpenSpec all describe the same `1.0.11` capability/release state.
- [ ] 22.5 Set the final actual release date in `CHANGELOG.md` only after all other gates pass, commit it, and rerun the complete validation because the SHA changed.
- [ ] 22.6 Tag `v1.0.11` only at the final validated SHA.

## 23. Post-publication verification

- [ ] 23.1 Verify the public npm registry reports `@vohongtho.infotech/code-intel@1.0.11`.
- [ ] 23.2 Install the public npm package in a fresh temporary environment and rerun version/doctor/analyze/MCP/server smoke tests.
- [ ] 23.3 Pull the published GHCR `1.0.11` image by immutable digest and run version/startup smoke tests.
- [ ] 23.4 Download published self-contained runtime artifact(s), verify checksum/SBOM/provenance and run the host-compatible bundle.
- [ ] 23.5 Verify GitHub Release artifacts correspond to the tagged SHA and release evidence.
- [ ] 23.6 If a critical defect is found after npm publication, do not overwrite `1.0.11`; prepare a corrective patch release (normally `1.0.12`) and stop promoting a defective mutable Docker alias where applicable.

## 24. Mandatory documentation completion

- [x] 24.1 Update `README.md` as specified in section 16 before marking this OpenSpec change complete. _(All of section 16 (16.1-16.8) is now complete.)_
- [x] 24.2 Update `CHANGELOG.md` as specified in section 17 before marking this OpenSpec change complete. _(All of section 17 (17.1-17.7) is now complete.)_
- [x] 24.3 Run OpenSpec validation after all requirement/task/spec changes. _(`openspec validate v1-0-11-release-readiness-hardening --strict --type change` passes; fixed two requirement-body wording errors it caught in specs/release-readiness/spec.md.)_
- [ ] 24.4 Archive `v1-0-11-release-readiness-hardening` only after release blockers are closed and the exact-SHA release gate has passed; if OpenSpec archival is intentionally performed only after publication, document that lifecycle explicitly. _(Not applicable yet — many release blockers remain open; do not archive.)_
