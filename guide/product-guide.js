(() => {
'use strict';
const replacements = [{"group": "Getting Started", "slug": "overview", "title": "Overview", "markdown": "# Code Intelligence Platform 1.0.9\n\nCode Intel builds a persistent knowledge graph from a repository and exposes it through a CLI, MCP server, local Web UI, and HTTP API.\n\n## Why use a knowledge graph?\n\nText search finds matching lines. Code Intel adds the relationships needed to change code safely:\n\n- callers and callees,\n- imports, inheritance, containment, and exports,\n- execution flows and functional clusters,\n- API routes,\n- tests and coverage relationships,\n- cross-repository contracts and dependencies.\n\nThe main goal is to let an AI agent understand **what a change affects before it edits code**.\n\n## Two ways to use Code Intel\n\n| Workflow | Best for |\n|---|---|\n| CLI + MCP | Daily development with Claude Code, Codex, Cursor, VS Code/Copilot, Cline, Windsurf, OpenCode, and other MCP clients |\n| Local Web UI + HTTP API | Visual exploration, source preview, graph queries, administration, and custom integrations |\n\nBoth workflows use the same local index under `.code-intel/`.\n\n## What your agent gets\n\nCode Intel 1.0.9 provides:\n\n- 31 MCP tools for search, symbol context, impact analysis, change review, quality, security, and repository groups,\n- 3 MCP resources for repository overview, clusters, and flows,\n- BM25 search plus optional vector/hybrid search,\n- bounded LLM-ready context documents,\n- generated agent instruction/rule files,\n- optional hooks and plugins installed by `code-intel setup`.\n\n## Recommended workflow\n\n```text\nAnalyze → Connect MCP → Explore → Inspect → Assess impact → Change → Review\n```\n\nStart with **Quick Start**, then follow **Agent Workflows** instead of calling tools randomly.\n"}, {"group": "Getting Started", "slug": "quick-start", "title": "Quick Start", "markdown": "# Quick Start\n\n## 1. Install\n\n```bash\nnpm install -g @vohongtho.infotech/code-intel@1.0.9\ncode-intel --version\n```\n\nNode.js 22 or newer is required.\n\n## 2. Index the repository\n\nFrom the repository root:\n\n```bash\ncode-intel analyze --name my-repository\n```\n\nOr pass an absolute path:\n\n```bash\ncode-intel analyze /absolute/path/to/repository --name my-repository\n```\n\nConfirm the index:\n\n```bash\ncode-intel status /absolute/path/to/repository\ncode-intel repo list\n```\n\nEmbeddings are optional. Enable them only when semantic/vector search is needed:\n\n```bash\ncode-intel analyze --embeddings\n```\n\n## 3. Connect an MCP client\n\nThe reliable Code Intel server command is:\n\n```bash\ncode-intel mcp /absolute/path/to/repository\n```\n\nGeneric stdio configuration:\n\n```json\n{\n  \"command\": \"code-intel\",\n  \"args\": [\"mcp\", \"/absolute/path/to/repository\"]\n}\n```\n\nUse an absolute path so the agent always opens the intended repository.\n\n## Verify from the agent\n\nAsk the agent to call:\n\n```text\noverview\nsearch\ninspect\ncontext\nblast_radius\n```\n\nFirst prompt:\n\n> Use Code Intel to explain the main modules, entry points, API routes, and execution flows in this repository. Do not edit files.\n\n## Optional Web UI\n\n```bash\ncode-intel serve /absolute/path/to/repository --port 4747\n```\n\nOpen `http://127.0.0.1:4747`.\n"}, {"group": "Concepts", "slug": "how-it-works", "title": "How It Works", "markdown": "# How Code Intel Works\n\n## Indexing pipeline\n\nA normal analysis:\n\n1. discovers source files and applies exclusions,\n2. parses supported languages,\n3. resolves symbols and cross-file relationships,\n4. builds graph structures, routes, clusters, and flows,\n5. builds the BM25 text index,\n6. optionally generates embeddings,\n7. persists the generation under `.code-intel/`,\n8. registers the repository globally,\n9. updates selected agent instruction/rule files unless disabled.\n\n## Search layers\n\n| Layer | Purpose |\n|---|---|\n| BM25 | Fast lexical search over names, paths, signatures, summaries, and code |\n| Vector | Semantic similarity when embeddings exist |\n| Hybrid | Reciprocal Rank Fusion of BM25 and vector results |\n| Graph | Callers, callees, relationship paths, flows, and blast radius |\n\nCLI `search` is BM25/text-oriented. MCP and HTTP expose BM25, vector-preferred, and hybrid modes.\n\n## Context builder\n\nThe `context` tool produces compact sections such as:\n\n```text\n[SUMMARY]\n[LOGIC]\n[RELATION]\n[FOCUS CODE]\n```\n\nIt deduplicates overlapping content and enforces a complete-document token budget.\n\n## Change behavior in 1.0.9\n\n- No detected source changes keep the fast path.\n- Any non-empty changed/deleted set performs a correctness-first graph rebuild.\n- Known changed/deleted vector scope is refreshed without rewriting unchanged vectors.\n- Missing, incompatible, or unknown vector state triggers a full vector rebuild.\n\nThis favors complete cross-file relationships over a partially updated graph.\n"}, {"group": "Agent Integration", "slug": "agent-workflows", "title": "Agent Workflows", "markdown": "# Agent Workflows\n\nUse these playbooks as the main guide for coding agents.\n\n## Explore an unfamiliar repository\n\n```text\noverview → clusters → flows → routes → search → inspect → context\n```\n\nPrompt:\n\n> Use Code Intel to map this repository. Identify the main clusters, execution flows, API routes, entry points, and important symbols before reading files directly.\n\n## Understand one symbol\n\n```text\ninspect → blast_radius → explain_relationship → find_path → context\n```\n\nPrompt:\n\n> Explain `PaymentService.process`: definition, callers, callees, flows, paths to persistence, and upstream blast radius.\n\n## Debug a failure\n\n```text\nsearch → routes or flows → find_path → explain_relationship → context → suggest_tests\n```\n\nPrompt:\n\n> Trace the failing request from the route to the likely data-access path. Show the call path, suspicious relationships, and tests that should reproduce the bug.\n\n## Assess impact before editing\n\n```text\ninspect → blast_radius → coverage_gaps → suggest_tests\n```\n\nPrompt:\n\n> Before changing `UserService.validate`, calculate upstream and downstream impact, list affected files and flows, identify untested callers, and warn about high-risk dependencies.\n\n## Review a diff or pull request\n\n```text\ndetect_changes or pr_impact → coverage_gaps → secrets → vulnerability_scan\n```\n\nPrompt:\n\n> Review this diff with Code Intel. Map changes to symbols and flows, rank risk, list files needing review, identify missing tests, and run security checks. Do not modify code.\n\n## Document architecture\n\n```text\noverview → clusters → cluster_summary → flows → routes\n```\n\nPrompt:\n\n> Produce an architecture map organized by functional cluster. Include major execution flows, API routes, and important files and symbols.\n\n## Multi-repository analysis\n\n```text\ngroup_status → group_contracts → group_query\n```\n\nPrompt:\n\n> Check the repository group for stale indexes, summarize contracts, and trace which services provide and consume the authentication APIs.\n\n## Suggested agent rule\n\n```text\nUse Code Intel before broad file search:\n1. overview/search to locate the concept.\n2. inspect/context for symbol understanding.\n3. blast_radius before changing shared symbols.\n4. detect_changes or pr_impact before finalizing.\n5. suggest_tests and coverage_gaps to validate the change.\nUse raw grep only when Code Intel results are insufficient, and state why.\n```\n"}];
const current = window.CODE_INTEL_AUDITED_PAGES || [];
for (const replacement of replacements) {
  const index = current.findIndex((page) => page.slug === replacement.slug);
  if (index >= 0) current[index] = replacement;
  else current.push(replacement);
}
const order = [
  'overview','quick-start','installation','how-it-works','analyze',
  'mcp-setup','integration-status','agent-integration','agent-workflows',
  'mcp-reference','search-context','cli-reference','web-ui','http-api',
  'repository-groups','quality-security','config-auth-backup',
  'openspec-integration','operations-runbook','known-limitations',
  'runtime-verified','source-verification'
];
const rank = new Map(order.map((slug, index) => [slug, index]));
current.sort((a, b) => (rank.get(a.slug) ?? 999) - (rank.get(b.slug) ?? 999));
window.CODE_INTEL_AUDITED_PAGES = current;
})();
