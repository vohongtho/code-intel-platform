window.CODE_INTEL_AUDITED_PAGES=(window.CODE_INTEL_AUDITED_PAGES||[]).concat([{"group": "User Interfaces", "slug": "web-ui", "title": "Web UI", "markdown": "# Web UI\n\nStart the local server:\n\n```bash\ncode-intel serve [path] --port 4747\n```\n\nBackground mode:\n\n```bash\ncode-intel serve --detach\ncode-intel stop\n```\n\nLive re-index mode:\n\n```bash\ncode-intel watch [path] --port 4747\n```\n\n## Verified application routes\n\nThe React app defines:\n\n```text\n/login\n/connect\n/loading\n/explore\n/settings/:section?\n```\n\nUnknown routes redirect to `/login` or `/connect` according to authentication state.\n\n## Explorer\n\nThe Explorer page includes:\n\n- graph visualization,\n- node detail,\n- source preview,\n- AI sidebar,\n- explorer composition and counts,\n- filters,\n- file browser,\n- GQL query panel,\n- repository-group panels,\n- keyboard shortcuts,\n- status footer.\n\n## Settings\n\nSettings sections are global to the local Code Intel server:\n\n- overview,\n- LLM,\n- embeddings,\n- analysis,\n- server,\n- authentication,\n- updates,\n- telemetry.\n\nAuthenticated viewers can read masked configuration. Only users with the `admin` role can save settings.\n\n## Authentication\n\nThe Web UI is route-guarded and expects an authenticated session. First-run bootstrap creates the first admin user. Local login and optional OIDC routes are implemented.\n\n## Source preview safety\n\nSource API requests validate repository access and restrict file reads to known repository paths.\n\n## Watch updates\n\n`code-intel watch` patches the live graph after file changes and broadcasts `graph:updated` over WebSocket. The watch path and Web UI repository should be checked explicitly."}, {"group": "CLI", "slug": "cli-reference", "title": "CLI Reference", "markdown": "# CLI Reference\n\nThis page lists the commands registered by `src/cli/main.ts`, `src/cli/app.ts`, and `src/cli/standalone-commands.ts`.\n\n## Global\n\n```bash\ncode-intel --version\ncode-intel --debug\n```\n\n## Initialization and configuration\n\n```bash\ncode-intel init [--reset] [--yes]\ncode-intel config get <key>\ncode-intel config set <key> <value>\ncode-intel config list\ncode-intel config validate\ncode-intel config reset [--yes]\ncode-intel config-validate <file>\ncode-intel completion bash|zsh|fish\ncode-intel setup [--completion]\n```\n\n## Analysis and servers\n\n```bash\ncode-intel analyze [path] [options]\ncode-intel mcp [path]\ncode-intel serve [path] [--port <n>] [--force] [--detach]\ncode-intel stop [path]\ncode-intel watch [path] [--port <n>] [--force]\n```\n\n## Repository registry and cleanup\n\n```bash\ncode-intel repo list\ncode-intel repo show <name>\ncode-intel repo rename <name> <newName>\ncode-intel repo relink <name> <newPath>\ncode-intel status [path-or-repo-name]\ncode-intel clean [path]\n```\n\n`clean` options:\n\n```text\n--all\n--force\n--purge\n--dry-run\n--list-trash\n```\n\nThere is no implemented top-level `code-intel list` command in 1.0.9, although stale help examples mention it. Use `code-intel repo list`.\n\nThere is no `repo remove`; cleanup removes the local index and registry entry.\n\n## Search and graph exploration\n\n```bash\ncode-intel search <query> [--limit <n>] [--path <path>] [--json]\ncode-intel inspect <symbol> [--path <path>] [--json]\ncode-intel impact <symbol> [--path <path>] [--depth <n>]\ncode-intel context <symbols...> [--path <path>] [--limit <n>] [--intent <intent>] [--max-tokens <n>] [--show-context]\ncode-intel deprecated [path] [--format table|json]\n```\n\nCLI `search` is BM25/text search. Vector-preferred and scoped search are exposed through MCP and HTTP.\n\n## GQL and saved queries\n\n```bash\ncode-intel query \"<gql>\"\ncode-intel query --file <path>\ncode-intel query --format table|json|csv\ncode-intel query --limit <n>\ncode-intel query --path <path>\ncode-intel query --save <name> \"<gql>\"\ncode-intel query --run <name>\ncode-intel query --list\ncode-intel query --delete <name>\n```\n\nSupported GQL statement families are `FIND`, `TRAVERSE`, `PATH`, and `COUNT`.\n\n## Change review\n\n```bash\ncode-intel pr-impact --base <ref> --head <ref>\ncode-intel pr-impact --fail-on HIGH|MEDIUM\ncode-intel pr-impact --format text|json|sarif\ncode-intel pr-impact --path <path>\n```\n\nStandalone change-context commands:\n\n```bash\ncode-intel index-status [path] [--upgrade-legacy]\ncode-intel change-context [path] --files a.ts,b.ts\ncode-intel change-context [path] --diff-file change.diff\ncode-intel change-context [path] [--max-hops <n>] [--max-tokens <n>] [--max-symbols <n>]\ncode-intel change-context-mcp [path]\ncode-intel change-context-http [path] [--host <host>] [--port <n>]\n```\n\n## Repository groups\n\n```bash\ncode-intel group create <name>\ncode-intel group add <group> <groupPath> <registryName>\ncode-intel group remove <group> <groupPath>\ncode-intel group list [name]\ncode-intel group sync <name> [--dry-run]\ncode-intel group contracts <name> [--kind <kind>] [--repo <repo>] [--min-confidence <pct>]\ncode-intel group query <name> <query> [--limit <n>]\ncode-intel group status <name>\ncode-intel group init-workspace [path] [--name <name>] [--no-analyze] [--yes] [--parallel <n>]\n```\n\nThere is no CLI `group delete` command in 1.0.9. The HTTP API does implement group deletion.\n\n## Quality and security\n\n```bash\ncode-intel health [path] [--dead-code] [--cycles] [--orphans]\ncode-intel complexity [path] [--top <n>] [--threshold <n>] [--format table|json] [--scope <prefix>]\ncode-intel coverage [path] [--threshold <pct>] [--format table|json] [--scope <prefix>]\ncode-intel secrets [path] [--format table|json] [--fail-on] [--fix-hint] [--include-tests]\ncode-intel scan [path] [--type <types>] [--severity high|medium|low] [--format table|json|sarif] [--fail-on high|medium] [--exclude <pattern>]\n```\n\n## Users, tokens, authentication\n\n```bash\ncode-intel user create <username> --role <role> [--password <password>]\ncode-intel user list\ncode-intel user delete <username>\ncode-intel user reset-password <username> [--password <password>]\ncode-intel user set-role <username> <role>\n\ncode-intel token create --name <name> --role <role> [--expires <duration>] [--repos <names>]\ncode-intel token list\ncode-intel token revoke <id>\n\ncode-intel auth login [--server <url>]\ncode-intel auth logout\ncode-intel auth rotate-token <id>\n```\n\nRoles are `admin`, `analyst`, `viewer`, and `repo-owner`.\n\n## Backup, migration, secrets\n\n```bash\ncode-intel backup create [path]\ncode-intel backup list\ncode-intel backup restore <id> [--target <path>]\ncode-intel migrate [--dry-run] [--status] [--rollback] [--db <path>]\ncode-intel keystore set <key> <value>\ncode-intel keystore get <key>\ncode-intel keystore delete <key>\ncode-intel keystore list\ncode-intel keystore backend\n```\n\n## Maintenance\n\n```bash\ncode-intel update [--yes] [--no-update-check]\ncode-intel doctor\n```\n\n## Hidden/internal commands\n\nThese are registered but hidden from normal help:\n\n```bash\ncode-intel rewrite \"<shell command>\"\ncode-intel hook claude\n```\n\n`code-intel-hook` is a separate lightweight binary supporting `claude`, `copilot`, `cursor`, and `gemini`."}, {"group": "MCP", "slug": "mcp-setup", "title": "MCP Setup", "markdown": "# MCP Setup\n\n## Server command\n\nThe MCP server uses stdio:\n\n```bash\ncode-intel mcp /absolute/path/to/repository\n```\n\nWhen a valid index exists, the graph is loaded from its database. If the index does not exist, `mcp` silently runs analysis before starting.\n\nUse an absolute repository path for deterministic behavior.\n\n## Built-in setup command\n\n```bash\ncode-intel setup\n```\n\nIn 1.0.9, `setup`:\n\n- prints and attempts to write a Claude-style MCP configuration,\n- prints a VS Code/Cursor `.vscode/mcp.json` example,\n- installs a Claude Code PreToolUse hook,\n- installs Cursor and Gemini hooks when their configuration directories exist,\n- writes a GitHub Copilot project hook,\n- installs OpenCode and OpenClaw plugins when detected,\n- writes rules for Cline/Roo, Windsurf, Kilo Code, Antigravity, and Codex.\n\nActual generated MCP command:\n\n```json\n{\n  \"command\": \"npx\",\n  \"args\": [\"code-intel\", \"mcp\", \".\"]\n}\n```\n\nThis generated command is a known 1.0.9 limitation because the published npm package is scoped. Manual configuration using the installed `code-intel` executable is more reliable.\n\n## Generic stdio configuration\n\n```json\n{\n  \"command\": \"code-intel\",\n  \"args\": [\"mcp\", \"/absolute/path/to/repository\"]\n}\n```\n\n## Claude Code\n\nCurrent Claude Code CLI syntax supports project scope:\n\n```bash\nclaude mcp add --scope project code-intel -- code-intel mcp \"$(pwd)\"\nclaude mcp list\nclaude mcp get code-intel\n```\n\nA shared project registration is stored in `.mcp.json`.\n\n## Codex\n\nA local stdio server can be registered through the Codex MCP CLI:\n\n```bash\ncodex mcp add code-intel -- code-intel mcp \"$(pwd)\"\ncodex mcp list\n```\n\nEquivalent TOML:\n\n```toml\n[mcp_servers.code-intel]\ncommand = \"code-intel\"\nargs = [\"mcp\", \"/absolute/path/to/repository\"]\n```\n\nCodex also reads repository instructions from `AGENTS.md`.\n\n## VS Code / GitHub Copilot\n\nWorkspace file `.vscode/mcp.json`:\n\n```json\n{\n  \"servers\": {\n    \"code-intel\": {\n      \"type\": \"stdio\",\n      \"command\": \"code-intel\",\n      \"args\": [\"mcp\", \"${workspaceFolder}\"]\n    }\n  }\n}\n```\n\nUse **MCP: List Servers** to start, restart, or inspect output.\n\n## Other clients\n\nFor Cursor, Cline, Windsurf, OpenCode, and other clients, register the same stdio command using the client's current MCP configuration schema. Client schemas are external to Code Intel and can change independently.\n\n## Authentication\n\nWhen `CODE_INTEL_TOKEN` is set on the MCP process, every tool invocation must include:\n\n```json\n{\n  \"_token\": \"the-configured-token\"\n}\n```\n\nThe standard MCP configuration does not automatically inject `_token` into tool arguments; use this mode only with a client/workflow able to supply it.\n\n## Verification\n\nAfter setup:\n\n1. Run `code-intel status /absolute/path/to/repository`.\n2. Confirm the client lists `code-intel`.\n3. Call `overview`.\n4. Call `search` for a known symbol.\n5. Inspect the reported repository.\n6. Restart the client and repeat."}, {"group": "MCP", "slug": "mcp-reference", "title": "MCP Tools & Resources", "markdown": "# MCP Tools and Resources\n\nThe 1.0.9 MCP server advertises **31 tools**.\n\n## Exact tool list\n\n```text\nrepos\noverview\nsearch\ninspect\ncontext\nblast_radius\nfile_symbols\nfind_path\nlist_exports\nroutes\nclusters\nflows\ndetect_changes\nquery\nraw_query\ngroup_list\ngroup_sync\ngroup_contracts\ngroup_query\ngroup_status\nexplain_relationship\npr_impact\nsimilar_symbols\nhealth_report\nsuggest_tests\ncluster_summary\ndeprecated_usage\ncomplexity_hotspots\ncoverage_gaps\nsecrets\nvulnerability_scan\n```\n\n## Search and understanding\n\n| Tool | Main inputs |\n|---|---|\n| `repos` | none |\n| `overview` | none |\n| `search` | `query`, `offset`, `limit`, `mode`, `scope` |\n| `inspect` | `symbol_name` |\n| `context` | `symbols`, `intent`, `max_tokens`, `limit` |\n| `blast_radius` | `target`, `direction`, `max_hops` |\n| `file_symbols` | `file_path`, `offset`, `limit` |\n| `find_path` | `from`, `to`, `max_hops` |\n| `list_exports` | `kind`, `offset`, `limit` |\n\n## Architecture and execution\n\n`routes`, `clusters`, `flows`, `cluster_summary`, `explain_relationship`, and `similar_symbols`.\n\n## Change review\n\n`detect_changes` analyzes a Git diff or supplied unified diff.\n\n`pr_impact` accepts changed files or diff text and reports risk, coverage gaps, files to review, and optional group impact.\n\n`suggest_tests` proposes test cases and reports existing/untested relationships.\n\n## Query\n\n`query` executes GQL (`FIND`, `TRAVERSE`, `PATH`, `COUNT`).\n\n`raw_query` supports only a simplified exact-name or kind query; it is not a full Cypher implementation.\n\n## Multi-repository\n\n`group_list`, `group_sync`, `group_contracts`, `group_query`, and `group_status`.\n\n## Quality and security\n\n`health_report`, `deprecated_usage`, `complexity_hotspots`, `coverage_gaps`, `secrets`, and `vulnerability_scan`.\n\n## MCP resources\n\n```text\ncodeintel://repo/<repo-name>/overview\ncodeintel://repo/<repo-name>/clusters\ncodeintel://repo/<repo-name>/flows\n```\n\nThe MCP server advertises tools and resources. It does not advertise MCP prompts in 1.0.9.\n\n## Timeout behavior\n\nDefault tool timeout:\n\n```text\n30000 ms\n```\n\nOverride:\n\n```bash\nexport CODE_INTEL_MCP_TIMEOUT_MS=60000\n```\n\nA timeout returns a non-error payload containing `truncated: true`, a reason, and an empty `partialResults` array.\n\n## Protocol version metadata\n\nThe package is 1.0.9, but the MCP server metadata is hardcoded as `0.1.0`. This is a known implementation inconsistency, not a separate installed package version."}]);
