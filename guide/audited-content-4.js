window.CODE_INTEL_AUDITED_PAGES=(window.CODE_INTEL_AUDITED_PAGES||[]).concat([{"group": "Quality & Security", "slug": "quality-security", "title": "Quality & Security", "markdown": "# Quality and Security\n\nThese commands operate on the indexed graph. They are static-analysis signals, not proof that code is safe, vulnerable, dead, or covered.\n\n## Health\n\n```bash\ncode-intel health\ncode-intel health --dead-code --cycles --orphans\n```\n\nHealth combines dead-code, cycle, god-node, orphan, and score/grade signals.\n\n## Complexity\n\n```bash\ncode-intel complexity --top 20\ncode-intel complexity --threshold 10\ncode-intel complexity --scope src/api\ncode-intel complexity --format json\n```\n\nResults include cyclomatic and cognitive complexity.\n\n## Coverage gaps\n\n```bash\ncode-intel coverage\ncode-intel coverage --threshold 80\ncode-intel coverage --scope src/api\n```\n\nCoverage is inferred from graph relationships and test-file conventions; it is not a replacement for instrumented runtime coverage.\n\n## Secret scanning\n\n```bash\ncode-intel secrets\ncode-intel secrets --include-tests\ncode-intel secrets --fail-on\n```\n\nImplemented detections include selected OpenAI, Stripe, AWS, Slack, database-URL, RSA-key, sensitive-name, and high-entropy patterns. Test/spec/fixture/mock files are excluded by default.\n\nFalse positives and false negatives are possible.\n\n## Vulnerability scanning\n\n```bash\ncode-intel scan\ncode-intel scan --type secrets,sql,xss,ssrf,path,cmd\ncode-intel scan --severity high\ncode-intel scan --fail-on medium\ncode-intel scan --format sarif\n```\n\nImplemented vulnerability families:\n\n| Type | CWE |\n|---|---|\n| SQL injection | CWE-89 |\n| XSS | CWE-79 |\n| SSRF | CWE-918 |\n| Path traversal | CWE-22 |\n| Command injection | CWE-78 |\n\nThe detector combines parser-produced security signals with generic heuristics. Findings must be reviewed in source context.\n\n## SARIF metadata limitation\n\nThe general `scan --format sarif` payload hardcodes tool version `0.8.0` in 1.0.9. The installed package version remains 1.0.9."}, {"group": "Operations", "slug": "configuration-auth-backup", "title": "Configuration, Auth & Backup", "markdown": "# Configuration, Authentication, and Backup\n\n## Global configuration\n\n```bash\ncode-intel init\ncode-intel config list\ncode-intel config get llm.provider\ncode-intel config set llm.provider ollama\ncode-intel config validate\n```\n\nConfiguration path:\n\n```text\n~/.code-intel/config.json\n```\n\nDefault sections:\n\n```text\nllm\nembeddings\nanalysis\nserve\nauth\nupdates\ntelemetry\n```\n\nSensitive values are masked when listed.\n\n## Local users and roles\n\nRoles:\n\n```text\nadmin\nanalyst\nviewer\nrepo-owner\n```\n\nCreate and manage users with the `user` commands. The first Web UI bootstrap user becomes an administrator.\n\n## Tokens\n\nAPI tokens may be assigned a role, expiry, and optional repository scope.\n\n```bash\ncode-intel token create --name ci --role analyst --expires 90d --repos api-core\n```\n\n## OIDC\n\nThe CLI and HTTP server implement OIDC login/device flows when issuer/client configuration is present. Local authentication remains available as the default mode.\n\n## Keystore\n\n```bash\ncode-intel keystore backend\n```\n\nThe active implementation is an encrypted file store under `.code-intel/.secrets`; command output describes the active backend.\n\n## Backups\n\n```bash\ncode-intel backup create /path/to/repo\ncode-intel backup list\ncode-intel backup restore <id>\n```\n\nActual backup candidates:\n\n```text\n<repo>/.code-intel/graph.db\n<repo>/.code-intel/vector.db\n<repo>/.code-intel/meta.json\n~/.code-intel/registry.json\n~/.code-intel/users.db\n```\n\nBackups are packed into an AES-256-GCM encrypted `.cib` archive with SHA-256 hashes in the manifest.\n\nDespite an older source comment mentioning config, `config.json` is not included in the actual `createBackup()` candidate list in 1.0.9.\n\nOptional S3 upload is controlled through `CODE_INTEL_BACKUP_S3_*` environment variables.\n\n## Backup key\n\nSet a stable 32-byte key as at least 64 hex characters:\n\n```bash\nexport CODE_INTEL_BACKUP_KEY=<64-hex-characters>\n```\n\nWithout it, a key is derived from hostname and home directory for development convenience; moving the archive to another machine can then prevent decryption."}, {"group": "Agent Integration", "slug": "agent-integrations", "title": "Agent Integrations", "markdown": "# Agent Integrations\n\nCode Intel integration has three separate layers:\n\n1. MCP server registration.\n2. Repository instruction files.\n3. Runtime hooks/plugins that rewrite selected shell discovery commands.\n\nDo not treat one layer as proof that the other two are installed.\n\n## Agent-target instruction files\n\nBuilt-in target paths include:\n\n| Agent | File |\n|---|---|\n| Claude Code | `CLAUDE.md` |\n| Codex | `AGENTS.md` |\n| GitHub Copilot | `.github/copilot-instructions.md` |\n| Cursor | `.cursor/rules/code-intel.mdc` |\n| Kiro | `.kiro/steering/code-intel.md` |\n| Cline | `.clinerules` |\n| Windsurf | `.windsurfrules` |\n| Roo Code | `.clinerules` |\n| Antigravity | `.agents/rules/code-intel-rules.md` |\n\nThe interactive analyzer can save selected targets in:\n\n```text\n.code-intel/agent-targets.json\n```\n\nOnly selected targets are generated/updated. Existing custom content is preserved through managed blocks where the context writer supports them.\n\n## Runtime setup actions\n\n`code-intel setup` attempts:\n\n| Client | Mechanism |\n|---|---|\n| Claude Code | global PreToolUse hook |\n| Cursor | global preToolUse hook |\n| Gemini CLI | global BeforeTool hook |\n| GitHub Copilot | project hook JSON |\n| OpenCode | global plugin |\n| OpenClaw | global extension |\n| Cline/Roo | project rules |\n| Windsurf | project rules |\n| Codex | `AGENTS.md` rules |\n| Kilo Code | project rules |\n| Antigravity | project rules |\n\nSkipped integrations print a reason when the expected client directory is absent.\n\n## Rewrite behavior\n\nThe separate `code-intel-hook` binary is intentionally lightweight. It supports:\n\n```text\nclaude\ncopilot\ncursor\ngemini\n```\n\nThe main CLI also has hidden `rewrite` and `hook claude` commands.\n\nHooks are non-blocking by design: failures should not stop the agent's original command.\n\n## Skills\n\nVersion 1.0.9 does **not** generate Agent Skills. `src/cli/skill-writer.ts` is a compatibility no-op that always returns an empty list.\n\nAny skills shown by an AI client come from the client, another plugin, or manually created files—not Code Intel 1.0.9.\n\n## Known path inconsistency\n\nKilo Code has different paths in selected-target and legacy target definitions. Treat `.kilocode/rules/code-intel-rules.md` as setup's actual rules path, and verify which path the installed client reads."}, {"group": "Spec-driven", "slug": "openspec-integration", "title": "OpenSpec Integration", "markdown": "# OpenSpec Integration\n\nOpenSpec is external to Code Intel. Code Intel 1.0.9 does not contain or execute `/opsx:*` commands.\n\nThis page describes a recommended workflow when an OpenSpec-capable agent and Code Intel MCP are both installed.\n\n## Responsibilities\n\n| OpenSpec | Code Intel |\n|---|---|\n| proposal and scope | repository discovery |\n| requirements/specs | symbol and dependency evidence |\n| design | relationship/path analysis |\n| tasks | blast-radius-informed implementation order |\n| verification | change impact, tests, health |\n| archive | final evidence and known limitations |\n\n## New feature\n\n```text\n/opsx:explore\n  → overview\n  → search\n  → inspect\n  → find_path / explain_relationship\n  → blast_radius\n\n/opsx:propose <change-name>\n  → use Code Intel evidence in proposal/spec/design/tasks\n\n/opsx:apply <change-name>\n  → context before editing\n  → detect_changes / pr_impact after editing\n  → suggest_tests\n  → health_report\n\n/opsx:verify <change-name>\n  → compare implementation against spec and acceptance criteria\n\n/opsx:archive <change-name>\n```\n\n## Bug fix\n\n```text\nsearch symptom\n→ inspect likely entry points\n→ explain_relationship\n→ find_path\n→ blast_radius\n→ focused context\n→ implement minimal fix\n→ detect_changes\n→ suggest_tests\n```\n\n## Refactor\n\n```text\ncomplexity_hotspots\n→ similar_symbols\n→ inspect\n→ blast_radius\n→ context\n→ implement in small tasks\n→ pr_impact\n→ coverage_gaps\n```\n\n## Security change\n\n```text\nsecrets / vulnerability_scan\n→ inspect evidence\n→ blast_radius\n→ proposal with threat and non-goals\n→ implement\n→ rescan\n→ suggest_tests\n```\n\n## Multi-repository change\n\n```text\ngroup_status\n→ group_sync\n→ group_contracts\n→ group_query\n→ per-repo inspect/context\n→ apply in dependency order\n→ group_sync again\n```\n\n## Requirement for accuracy\n\nOpenSpec command names and artifact profiles vary by installed OpenSpec version. Treat the `/opsx:*` names above as examples and verify them in the installed OpenSpec client. Code Intel tool names in this workflow are the exact 1.0.9 MCP names."}]);
