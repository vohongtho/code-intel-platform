window.CODE_INTEL_AUDITED_PAGES=(window.CODE_INTEL_AUDITED_PAGES||[]).concat([{"group": "Operations", "slug": "troubleshooting", "title": "Troubleshooting & Runbook", "markdown": "# Troubleshooting and Runbook\n\n## Confirm package and path\n\n```bash\nwhich code-intel\ncode-intel --version\nnode --version\n```\n\n## Confirm index state\n\n```bash\ncode-intel status /absolute/path/to/repository\ncode-intel index-status /absolute/path/to/repository\n```\n\n`index-status` exit behavior:\n\n- `0`: trusted/current,\n- `1`: stale or legacy,\n- `2`: missing or corrupt.\n\nUpgrade legacy metadata only when appropriate:\n\n```bash\ncode-intel index-status . --upgrade-legacy\n```\n\n## Repair sequence\n\n```bash\ncode-intel backup create .\ncode-intel clean --dry-run\ncode-intel clean --purge\ncode-intel analyze --embeddings\ncode-intel status\n```\n\nDo not restore an old backup over a newer healthy index without reviewing schema/version implications.\n\n## MCP starts with wrong repository\n\nUse an absolute path:\n\n```bash\ncode-intel mcp /absolute/path/to/repository\n```\n\nThen call `overview` and inspect the repository name and graph counts.\n\n## MCP connection closes\n\nRun the exact command in a terminal only as a diagnostic. A stdio server normally expects a connected MCP client; terminal behavior alone is not a complete protocol test.\n\nCheck:\n\n```text\n~/.code-intel/logs/\n```\n\nAlso verify the client can locate the same Node.js/npm/global binary environment.\n\n## Vector fallback\n\nRun:\n\n```bash\ncode-intel analyze --embeddings\ncode-intel status\n```\n\nThen use MCP/HTTP search and inspect:\n\n```text\nrequestedMode\nactualMode\nfallbackReason\nvectorReady\n```\n\nA requested vector search can correctly return `actualMode: hybrid`.\n\n## Web UI opens a different repository\n\n`serve` can fall back to the most recently indexed repository. Start it with an explicit path:\n\n```bash\ncode-intel serve /absolute/path/to/repository\n```\n\n## Database locking\n\nAvoid running destructive rebuild, cleanup, restore, and active writers against the same repository index simultaneously. Stop `watch` or detached `serve` before destructive repair.\n\n## Watcher issues\n\nRestart:\n\n```bash\ncode-intel watch /absolute/path/to/repository\n```\n\nIf accuracy is uncertain:\n\n```bash\ncode-intel analyze --force\ncode-intel watch\n```\n\n## CI change context\n\n```bash\ngit diff origin/main...HEAD > change.diff\ncode-intel change-context . --diff-file change.diff --max-tokens 6000\ncode-intel pr-impact --base origin/main --head HEAD --format sarif\n```\n\n## Release validation evidence\n\nThe source commit used for this guide completed the repository's Test, Quality, Release Readiness, Code Intel PR Impact, and source snapshot workflows successfully."}, {"group": "Audit", "slug": "known-limitations", "title": "Known 1.0.9 Inconsistencies", "markdown": "# Known 1.0.9 Inconsistencies\n\nThese are implementation facts found during the documentation audit.\n\n## Version strings\n\n- npm package: `1.0.9`\n- MCP server metadata: `0.1.0`\n- `scan --format sarif` tool metadata: `0.8.0`\n\nUse `code-intel --version` for the installed package version.\n\n## Setup package command\n\n`code-intel setup` generates:\n\n```text\nnpx code-intel mcp .\n```\n\nThe published package is scoped:\n\n```text\n@vohongtho.infotech/code-intel\n```\n\nPrefer the installed `code-intel` executable or use the full scoped npm package manually.\n\n## Stale help examples\n\nThe CLI help text contains examples for:\n\n```text\ncode-intel list\n```\n\nThe implemented registry command is:\n\n```text\ncode-intel repo list\n```\n\n## Group deletion\n\nThe HTTP API implements group deletion. The CLI does not.\n\n## Backup source comment\n\nThe backup file comment says config is archived, but `createBackup()` does not add `config.json` to its actual file candidate list.\n\n## Skills\n\nAgent skill generation was removed. A compatibility function remains but generates no files.\n\n## MCP authentication ergonomics\n\nWhen `CODE_INTEL_TOKEN` is enabled, `_token` must be supplied in each tool's arguments. Standard static MCP client environment configuration alone does not satisfy that tool argument requirement.\n\n## Security scanner scope\n\nSecret and vulnerability scanning are heuristic. The secret scanner uses selected value/name/entropy rules; the vulnerability detector combines structured parser signals and generic sink heuristics.\n\n## Documentation policy\n\nThe guide reports these inconsistencies rather than hiding them or documenting intended behavior as if already implemented."}, {"group": "Audit", "slug": "source-verification", "title": "Source Verification", "markdown": "# Source Verification\n\n## Audited source branch\n\n```text\nRepository: vohongtho/code-intel-platform\nBranch: release/1.0.9\nCommit: eeb6e0941c73958709ba83f09612693c66d07b8c\n```\n\n## CI evidence for that commit\n\nThe following GitHub Actions runs completed successfully:\n\n```text\nTest\nQuality\nRelease Readiness\nCode Intel PR Impact\nExport Source Snapshot\n```\n\n## Source areas checked\n\n### Package and build\n\n```text\npackage.json\ncode-intel/core/package.json\ncode-intel/shared/package.json\ncode-intel/web/package.json\ntsconfig*.json\n```\n\n### CLI and integrations\n\n```text\ncode-intel/core/src/cli/main.ts\ncode-intel/core/src/cli/app.ts\ncode-intel/core/src/cli/standalone-commands.ts\ncode-intel/core/src/cli/init-wizard.ts\ncode-intel/core/src/cli/agent-targets.ts\ncode-intel/core/src/cli/hook.ts\ncode-intel/core/src/cli/hook-rewriter.ts\ncode-intel/core/src/cli/context-writer.ts\ncode-intel/core/src/cli/skill-writer.ts\ncode-intel/core/src/cli/update-checker.ts\n```\n\n### Analysis, graph, search, and context\n\n```text\ncode-intel/core/src/pipeline/**\ncode-intel/core/src/graph/**\ncode-intel/core/src/storage/**\ncode-intel/core/src/search/**\ncode-intel/core/src/context/**\ncode-intel/core/src/query/**\ncode-intel/core/src/languages/**\ncode-intel/shared/src/languages.ts\ncode-intel/shared/src/detection.ts\n```\n\n### MCP and HTTP\n\n```text\ncode-intel/core/src/mcp-server/server.ts\ncode-intel/core/src/http/app.ts\ncode-intel/core/src/http/openapi.ts\ncode-intel/web/src/App.tsx\ncode-intel/web/src/pages/**\ncode-intel/web/src/components/**\ncode-intel/web/src/api/**\n```\n\n### Multi-repository, auth, operations, quality\n\n```text\ncode-intel/core/src/multi-repo/**\ncode-intel/core/src/auth/**\ncode-intel/core/src/backup/**\ncode-intel/core/src/migrations/**\ncode-intel/core/src/security/**\ncode-intel/core/src/health/**\ncode-intel/core/src/analysis/**\ncode-intel/core/src/jobs/**\ncode-intel/core/src/observability/**\n```\n\n### Tests and release evidence\n\n```text\ncode-intel/core/tests/**\ncode-intel/web/src/**/*.test.*\n.github/workflows/**\ndocs/releases/v1.0.9.md\nCHANGELOG.md\n```\n\n## Verification rule\n\nA claim is included as product behavior only when it is supported by source, tests, release documentation, or a successful runtime/CI contract.\n\nExternal client and OpenSpec instructions are labeled as external examples and are not presented as Code Intel implementation.\n\n## Honest boundary\n\nNo documentation audit can prove every runtime behavior for every operating system, source language, repository shape, and third-party client. This guide therefore avoids the phrase “guaranteed 100% at runtime.” It is a source-anchored reference for the exact audited release, with known inconsistencies disclosed explicitly."}]);
