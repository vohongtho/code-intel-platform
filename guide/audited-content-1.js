window.CODE_INTEL_AUDITED_PAGES=(window.CODE_INTEL_AUDITED_PAGES||[]).concat([{"group": "Getting Started", "slug": "overview", "title": "Overview", "markdown": "# Code Intelligence Platform 1.0.9\n\nCode Intelligence Platform is a local static-analysis platform. It parses supported source files, builds a persisted knowledge graph, and exposes repository intelligence through:\n\n- a CLI,\n- a local HTTP API and React Web UI,\n- an MCP stdio server,\n- repository-group and change-impact workflows.\n\n## Verified package identity\n\n```text\nPackage: @vohongtho.infotech/code-intel\nVersion: 1.0.9\nNode.js engine: >=22.0.0\nCLI binaries: code-intel, code-intel-hook\n```\n\n## Supported source languages\n\nThe parser registry in `code-intel/core/src/languages/registry.ts` registers:\n\nTypeScript, JavaScript, Python, Java, Go, C, C++, C#, Rust, PHP, Kotlin, Ruby, Swift, Dart, and HTML.\n\n## Core model\n\nThe graph contains source-level nodes such as files, classes, functions, methods, interfaces, types, constants, routes, flows, clusters, vulnerabilities, and supporting relationships such as calls, imports, extends, implements, contains, belongs-to, and security edges.\n\nThe exact nodes and edges available for a repository depend on language support and what the parser can infer from the source.\n\n## Recommended workflow\n\n```text\nAnalyze → Search → Inspect → Impact → Context → Change → Validate\n```\n\n## Version 1.0.9 focus\n\nVersion 1.0.9 separates graph execution scope from vector update scope:\n\n- a non-empty detected source change uses a correctness-first full graph rebuild,\n- changed-file vectors are deleted and regenerated,\n- deleted-file vectors are removed,\n- unchanged vectors are preserved,\n- a zero-change run performs no vector writes,\n- first use, `--force`, missing vector storage, stale/incompatible metadata, or unknown change scope triggers a full vector rebuild.\n\n## Documentation authority\n\nThis guide is tied to source branch `release/1.0.9`. The **Source Verification** page lists the files and automated checks used to keep the documentation aligned."}, {"group": "Getting Started", "slug": "installation", "title": "Installation & Requirements", "markdown": "# Installation and Requirements\n\n## Required\n\nThe published package declares only this runtime engine requirement:\n\n```text\nNode.js >= 22.0.0\n```\n\nnpm is required when installing from npm, but the package does not declare a specific npm engine version.\n\nGit is strongly recommended. Git-aware incremental detection, `pr-impact`, and several change-review workflows require a Git repository and Git executable. Basic analysis can still run without Git.\n\n## Install the exact release\n\n```bash\nnpm install -g @vohongtho.infotech/code-intel@1.0.9\ncode-intel --version\n```\n\nExpected version:\n\n```text\n1.0.9\n```\n\n## Install from source\n\n```bash\ngit clone --branch release/1.0.9 https://github.com/vohongtho/code-intel-platform.git\ncd code-intel-platform\nnpm install\nnpm run build\nnpm install -g ./code-intel/core\ncode-intel --version\n```\n\nThe root build runs the shared package, the core package, and the Web UI build.\n\n## Optional embedding dependency\n\n`@huggingface/transformers` is an optional dependency. Semantic embedding generation may install native/runtime dependencies such as ONNX Runtime and Sharp. Review install-script policy in controlled environments.\n\n## Upgrade\n\n```bash\nnpm install -g @vohongtho.infotech/code-intel@1.0.9\ncode-intel --version\n```\n\nA forced rebuild is **not required only because of the 1.0.9 vector planner change**. Use `code-intel analyze --force` when the index is missing, corrupted, built by an incompatible parser/schema, or when you intentionally require a complete reset.\n\n## Diagnostics\n\n```bash\ncode-intel doctor\n```\n\n`doctor` checks Node.js, Git, global configuration, repository registry, graph/vector database readability, index age, and npm registry reachability. Warnings do not always set a failing exit code; database/config failures do."}, {"group": "Getting Started", "slug": "quick-start", "title": "Quick Start", "markdown": "# Quick Start\n\nFrom the target repository:\n\n```bash\ncd /absolute/path/to/repository\ncode-intel analyze\ncode-intel status\n```\n\nEnable remembered semantic embeddings:\n\n```bash\ncode-intel analyze --embeddings\n```\n\nStart the Web UI:\n\n```bash\ncode-intel serve\n```\n\nOpen:\n\n```text\nhttp://localhost:4747\n```\n\nRun basic CLI exploration:\n\n```bash\ncode-intel search \"authentication\"\ncode-intel inspect AuthService\ncode-intel impact AuthService\ncode-intel context AuthService --show-context\n```\n\nStart the MCP stdio server directly:\n\n```bash\ncode-intel mcp /absolute/path/to/repository\n```\n\nOr configure supported editor integrations:\n\n```bash\ncode-intel setup\n```\n\n## Important repository-selection behavior\n\n`code-intel serve` does not always fail when the current directory has no index. It may fall back to the most recently indexed repository, or start with an empty graph when none exists. Check the startup output and selected repository before relying on results.\n\n`code-intel mcp <path>` is safer for agent integrations because the repository path is explicit."}, {"group": "Analysis", "slug": "analyze", "title": "Analyze & Index Lifecycle", "markdown": "# Analyze and Index Lifecycle\n\n## Command\n\n```bash\ncode-intel analyze [path]\n```\n\nImportant options:\n\n```text\n--name <name>\n--force\n--incremental\n--parallel\n--embeddings\n--skip-embeddings\n--skip-agents-md\n--skip-git\n--skip-folders <patterns...>\n--skip-files <patterns...>\n--verbose\n--summarize\n--llm-provider <provider>\n--llm-model <model>\n--llm-base-url <url>\n--llm-api-key <key>\n--llm-batch-size <n>\n--llm-max-nodes <n>\n--no-group-sync\n--dry-run\n--max-memory <MB>\n--profile\n```\n\n## What analysis writes\n\nDepending on options and available parsers, analysis creates or refreshes data under `.code-intel/`, including graph metadata/databases, BM25 data, optional vector data, profiling output, and selected agent instruction files.\n\nThe repository registry is stored globally under the user Code Intel directory.\n\n## Change detection\n\n`decideIncremental()` combines:\n\n- `git diff --name-only <previous-hash> --`,\n- untracked files from `git ls-files --others --exclude-standard`,\n- stored mtime comparisons,\n- deleted paths inferred from the previous mtime snapshot.\n\nThis covers committed differences from the stored base, staged/unstaged changes visible to `git diff`, untracked files, mtime-only changes, and deleted files.\n\n## Graph behavior in 1.0.9\n\nThe zero-change fast path remains incremental.\n\nAny non-empty changed/deleted set falls back to a clean full graph rebuild. This is intentional: removing and re-resolving only changed nodes can lose cross-file calls, imports, inheritance, clusters, and flows.\n\n## Vector behavior in 1.0.9\n\nVector planning is independent from the full graph rebuild:\n\n| Situation | Vector action |\n|---|---|\n| no source changes | no vector writes |\n| known changed/deleted paths | delete affected vectors and upsert changed-file vectors |\n| first embedding build | full vector build |\n| `--force` | full vector build |\n| vector storage missing | full vector build |\n| stale/incompatible metadata | full vector build |\n| unknown change scope | full vector build |\n\n## Embedding preference\n\nRunning with `--embeddings` records an enabled embedding preference for the repository. Future normal analyses maintain that vector index. `--skip-embeddings` skips vectors for the current run.\n\n## Exclusions\n\nUse command options for one run:\n\n```bash\ncode-intel analyze --skip-folders tests generated --skip-files \"*.generated.ts\"\n```\n\nUse `.codeintelignore` and `.codeintelignore.local` for repository/local exclusion rules. Exact pattern handling is implementation-specific; do not assume every Git ignore feature is supported.\n\n## Profiling and memory\n\n```bash\ncode-intel analyze --profile\ncode-intel analyze --max-memory 2048\n```\n\nProfiling writes `.code-intel/profile.json`. Memory limiting allows graph content to be spilled rather than retained entirely in memory."}]);
