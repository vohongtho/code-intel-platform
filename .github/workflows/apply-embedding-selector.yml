name: Apply embedding model selector

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Retired; implementation is validated by apply-v1-0-10-four-proposals.yml"
