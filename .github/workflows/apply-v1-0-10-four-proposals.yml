name: Apply v1.0.10 four proposals

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Retired; proposals are implemented and validated independently."
