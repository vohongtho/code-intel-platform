name: Apply v1.0.10 four proposals

on:
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: read

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow paused while restoring atomic staging payload"
